node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# VS Code local workspace (unrelated project paths)
*.code-workspace

# Runtime SQLite database (contains personal finance data)
finance.db

# Backups and exports — real ledgers, never the repository's business.
# A .db here once reached a public remote; the rule is the whole shape, not
# one filename, because the next one will be named something else.
DB Backup/
*.db
*.sqlite
*.smbk

# PWA dev service worker output
dev-dist/

# Editor settings (personal)
.vscode/
