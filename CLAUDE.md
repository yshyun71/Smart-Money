# 스마트 머니 — 개발 지침서

이 문서는 **이 문서만 가지고 같은 프로그램을 다시 만들 수 있도록** 구조·규칙·판단 근거를 기록한 것입니다.
코드를 고칠 때는 아래 "원칙"과 14절 "함정"을 먼저 읽으세요.

> **이 문서를 코드와 함께 고칩니다.**
> 프로그램을 바꾸면 영향받는 절을 같은 커밋에서 갱신합니다. 문서가 코드와 어긋나는 순간
> 이 문서는 목적을 잃고, 다음 작업이 잘못된 전제 위에서 시작됩니다.
> 자주 바뀌는 곳: **4.3** 마이그레이션 이력 · **4.4** 테이블 DDL · **6** 카테고리 ·
> **7** 가져오기 규칙 · **9** 카드 연결 · **12** 화면 구성 · **14** 함정(새로 겪은 사고).
>
> **명세서 형식을 건드렸다면 `tests/statements.test.ts`에 케이스를 추가하고
> `npm run check`를 돌린 뒤 커밋합니다.** 한 카드사를 지원하려다 다른 카드사를
>깨뜨리는 일이 반복해서 있었습니다.

---

## 1. 이 앱이 무엇인가

한국 개인용 가계부 PWA입니다. 은행 통장과 카드사 명세서를 **엑셀·CSV로 가져와** 분류하고, 고정비/변동비를 가려내고, 예산과 절약을 관리합니다.

세 가지가 이 앱의 성격을 결정합니다.

1. **서버가 없습니다.** 모든 데이터는 기기 안 SQLite(sql.js, WebAssembly)에 있고 IndexedDB에 저장됩니다. 백엔드도, 계정 서버도 없습니다.
2. **AI는 사용자의 키로 브라우저에서 직접 호출합니다.** 키는 기기에만 있고, AI 없이도 앱의 모든 기본 기능이 동작해야 합니다.
3. **금융기관 자동 연동은 없습니다.** 오픈뱅킹은 사업자 등록이, 스크래핑은 마이데이터 라이선스가 필요합니다. 개인 사용자는 **명세서 파일 가져오기**가 유일한 현실적 경로입니다. 이 전제를 뒤집지 마세요.

---

## 2. 기술 스택과 명령

| 항목 | 내용 |
| --- | --- |
| 프레임워크 | React 19 + TypeScript + Vite 6 |
| 스타일 | Tailwind CSS 4 (`@tailwindcss/vite`) |
| 아이콘 | `lucide-react` |
| 차트 | `recharts` |
| DB | `sql.js` 1.14 (WASM SQLite) + IndexedDB |
| 엑셀 | `xlsx` **0.20.3, npm 아닌 SheetJS CDN tarball** |
| PWA | `vite-plugin-pwa` (`registerType: 'autoUpdate'`) |
| AI SDK | `@google/genai`, `@anthropic-ai/sdk`, OpenAI는 fetch |
| 배포 | Cloudflare Workers(정적 자산) + GitHub `main` 푸시 |

빌드 시각이 번들에 박혀(`__BUILD_TIME__`, `vite.config.ts`의 `define`) **카드·계좌 화면의 DB 카드에 표시**됩니다. PWA는 한 번 실행에서 새 버전을 받고 다음 실행에서 보여주기 때문에, "고친 게 반영된 버전인가"를 기기에서 확인할 방법이 필요합니다.

```bash
npm run dev      # 개발 서버 — 반드시 http://localhost:3000 (14.9)
npm run lint     # tsc --noEmit
npm test         # 회귀 세트 7종 — 명세서·카드연결·일괄적용·표시형식·백업·예산·문자 (15절)
npm run check    # lint + test — 커밋 전에 이것을 돌립니다
npm run build    # vite build → dist/
```

**의존성 주의**
- `xlsx`는 반드시 `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`로 설치합니다. npm 레지스트리의 `xlsx@0.18.5`에는 미패치 취약점 2건이 있습니다.
- `@types/react`가 **없습니다.** 아래 "함정" 절을 반드시 읽으세요.

---

## 3. 디렉터리 구조

```
src/
├── main.tsx                  ErrorBoundary로 감싼 진입점
├── App.tsx                   인증 게이트 + 탭 전환 + 프레임/와이드 모드
├── types/finance.ts          모든 도메인 타입
├── constants/categories.ts   카테고리 목록·키워드 표 (단일 출처)
├── db/
│   ├── schema.ts             SCHEMA_VERSION · MIGRATIONS · 자가복구 · 샘플 데이터
│   ├── database.ts           sql.js 초기화 · IndexedDB 저장 · 통계 · 내보내기/가져오기
│   └── repository.ts         모든 SQL. 화면은 이 파일만 통해 DB에 접근
├── context/
│   ├── AuthContext.tsx       사용자 등록·로그인·로그아웃
│   └── FinanceContext.tsx    앱 상태의 중심. 모든 쓰기 동작이 여기를 지남
├── services/
│   ├── csvImport.ts          파일 해독 · 표 파싱 · 열 매칭 · 초안 생성
│   ├── statementFormats.ts   확인된 명세서 형식 기억 (localStorage)
│   ├── categoryRules.ts      패턴 규칙 · 카드대금/금융 자동 판별
│   ├── cardLink.ts           카드-출금 연결 · 결제월 대조 · 청구예정액
│   ├── balance.ts            잔액 조정 산식
│   ├── recurrence.ts         고정비 반복 판정
│   ├── pinCrypto.ts          PBKDF2 PIN 해시
│   ├── aiClient.ts           프롬프트 + JSON 스키마 (기능별)
│   └── ai/                   공급자 어댑터 계층
├── components/
│   ├── auth/ dashboard/ layout/ modals/ pwa/ settings/ transactions/ views/
│   └── ErrorBoundary.tsx
└── utils/
    ├── format.ts             금액·기준일시 포맷
    └── accountTone.ts        계좌/카드 색상
```

**계층 규칙**: 화면 → Context → repository → database. 화면이 `repository`나 `database`를 직접 부르지 않습니다(내보내기/가져오기 등 일부 관리 기능 예외). `services/`는 순수 로직이며 React를 모릅니다.

---

## 4. 데이터베이스

### 4.1 원칙

- **DB 파일을 배포하지 않습니다.** 최초 실행 시 기기에서 만들어지고, 그 뒤로는 프로그램만 교체됩니다.
- 구조 변경은 **마이그레이션 추가로만** 합니다. 이미 배포된 마이그레이션은 절대 수정하지 않습니다.
- 버전은 SQLite의 `PRAGMA user_version`에 있습니다. `.db` 파일을 내보냈다 가져와도 따라갑니다.
- 모든 사용자 데이터에는 `user_id`가 있고, `repository.ts`의 `requireUser()`가 모든 질의에 붙입니다. **범위 없는 질의가 나올 수 없는 구조**입니다.

### 4.2 자가 복구 (중요)

실제로 **버전만 올라가고 컬럼이 없는 기기**가 발생한 적이 있습니다(`pin_hash` 누락). 그 상태는 버전만 믿는 코드로는 영원히 복구되지 않습니다. 그래서:

- `EXPECTED_COLUMNS` — 있어야 할 컬럼 목록. 매 실행마다 `repairMissingColumns()`가 확인하고 없으면 추가합니다.
- `EXPECTED_TABLES` — 나중에 생긴 테이블의 DDL. `repairMissingTables()`가 없으면 만듭니다.
- `addColumn()`은 먼저 존재를 확인하고 ALTER 합니다(중복 실행 안전).

**새 컬럼·테이블을 추가하면 이 두 목록에도 반드시 넣으세요.**

### 4.3 마이그레이션 이력

| v | 내용 |
| --- | --- |
| 1 | 초기 스키마 (users, categories, accounts, transactions, budgets, budget_configs, ai_analyses) |
| 2 | PIN을 평문 대신 PBKDF2 해시로 (`pin_hash`, `pin_salt`, `pin_iterations`, 평문 `pin` 제거) |
| 3 | 사용자별 분리 — accounts/transactions에 `user_id` 추가, budgets/budget_configs/ai_analyses는 복합 PK로 **재구축**(SQLite는 PK 변경 불가) |
| 4 | `accounts.balance_as_of`, `balance_source`, `category_rules` 테이블 |
| 5 | `custom_categories` 테이블, 기본 카테고리에 `카드대금` |
| 6 | `금융/보험` → `보험`·`대출`·`기타 금융` 분리 (내역·규칙·예산 이관) |
| 7 | `주거/통신` → `주거`·`통신`, `생활/의료` → `생활`·`의료` 분리 |
| 8 | `transactions.linked_account_id` (카드대금이 결제하는 카드) |
| 9 | `transactions.billing_month` (결제월) |
| 10 | `accounts.payment_account_id`, `payment_account_label` (카드의 결제 계좌) |
| 11 | `transactions.note` (사용자가 직접 적는 설명) |
| 12 | `budget_policy` 테이블, `budget_configs.income_source`·`fixed_source` |
| 13 | `budget_configs.savings_source` |
| 14 | `저축` 카테고리 신설 — 적금·예금·청약 **지출**을 `기타 금융`에서 이관 |
| 15 | `sms_inbox` 테이블, `transactions.origin` (SMS/STATEMENT/MANUAL) |
| 16 | `budget_configs.income_excluded`·`fixed_excluded`·`savings_excluded` (실적에서 뺀 거래 id) |

현재 `SCHEMA_VERSION = 16`.

### 4.4 테이블 (현재 형태)

```sql
users(id PK, name, email, phone, pin, pin_hash, pin_salt, pin_iterations,
      auth_provider, provider_label, is_authenticated, is_biometric_enabled,
      authenticated_at, created_at)

categories(id PK, name UNIQUE, type, icon, color, is_default)   -- 앱은 읽지 않음. 참고용
custom_categories(id PK, user_id, name, type, created_at, UNIQUE(user_id, name))

accounts(id PK, user_id, name, type, institution, identifier,
         balance_or_billed, balance_as_of, balance_source,
         payment_account_id, payment_account_label,
         color, is_auto_sync_enabled, last_synced_at, created_at)

transactions(id PK, user_id, date, time, type, expense_type, category, merchant,
             amount, payment_method, account_id, memo, note,
             is_fixed_recurring, recurring_day,
             linked_account_id, billing_month, created_at)

category_rules(id PK, user_id, account_id, pattern, category, source,
               created_at, updated_at)

-- 확인 전의 문자. 등록해야 transactions 로 갑니다 (7.8)
sms_inbox(id PK, user_id, received_at, raw_text, parsed_json,
          status, created_at)

budgets(user_id, month, category, amount, PK(user_id, month, category))
budget_configs(user_id, month, monthly_income, fixed_expenses, savings_target,
               alert_threshold_percent, enable_push_alerts,
               income_source, fixed_source, savings_source,
               income_excluded, fixed_excluded, savings_excluded,   -- JSON 배열
               updated_at, PK(user_id, month))

-- 달에 매이지 않습니다. 사용자당 한 행 (11.5)
budget_policy(user_id PK, mode, rules_json, updated_at)
ai_analyses(user_id, month, analysis_json, health_score, updated_at, PK(user_id, month))
```

- `accounts.type`은 타입 선언상 `"BANK" | "CREDIT_CARD" | "CHECK_CARD"`이지만 **실제 저장값은 `"BANK"` 또는 `"CARD"`** 입니다. 판별은 항상 `type === "BANK"` / `type !== "BANK"`로 하세요.
- 날짜는 `YYYY-MM-DD`, 시각은 `HH:mm`, 결제월은 `YYYY-MM`, 기준일시는 ISO 문자열입니다.
- **`transactions.origin`** 은 그 줄이 어디서 왔는지입니다 — `SMS`(문자, **임시**) · `STATEMENT`(명세서) · `MANUAL`(직접 입력). 문자 건은 나중에 명세서가 대체해야 하므로 출처를 모르면 그 판단을 할 수 없습니다(7.8).
- **`memo`와 `note`는 주인이 다릅니다.** `memo`는 **명세서가 적어 준 것**(`구분`, 할부 회차 `4/10`)이고 가져오기가 씁니다. `note`는 **사람이 적는 설명**입니다. 절대 합치지 마세요 — `memo`의 회차가 중복 판정 키(7.6)와 `할부` 필터(9.5)의 근거라, 사람이 그 칸에 글을 쓰면 다음 달 같은 할부가 중복으로 걸러집니다.

### 4.5 백업 파일 암호화 (`services/backupCrypto.ts`)

**백업 파일은 이 기기를 떠나는 유일한 물건입니다** — 다운로드 폴더, 메일 첨부, 동기화 폴더. 그래서 암호를 걸 수 있습니다.

```
[SMBK1 5B][반복횟수 4B][salt 16B][IV 12B][AES-256-GCM 암호문]
```

- 키는 **PBKDF2-HMAC-SHA256 600,000회**로 파생하고 본문은 **AES-256-GCM**입니다. 반복 횟수를 파일에 적어 두므로 나중에 올려도 이미 만든 백업이 열립니다.
- **GCM을 쓰는 이유**: 암호가 틀리거나 한 바이트라도 바뀌면 복호화가 **실패**합니다. 그럴듯한 쓰레기를 돌려주지 않으므로, 그것을 DB로 여는 사고(4.6)가 나지 않습니다.
- **암호에 제한을 두지 않습니다.** 길이·구성·금지 패턴 전부 없습니다. 그런 규칙은 조건을 겨우 만족하는 가장 짧은 값을 고르게 만들고, 앱은 그 사람이 무엇을 기억할 수 있는지 알지 못합니다. **앞뒤 공백도 임의로 떼지 않습니다** — 나중에 열리지 않게 됩니다. 대신 `passphraseAdvice`가 얼마나 약한지 **말해 줄 뿐 막지는 않습니다**.
  → 6자리 숫자는 가능한 값이 100만 가지입니다. 이 PC 1코어에서 PBKDF2 600,000회가 316ms였으니 전수 탐색이 88시간, 8코어면 11시간, GPU면 분 단위입니다. PIN을 그대로 써도 되지만 안전하지는 않다는 뜻이고, 화면에도 그렇게 적혀 있습니다.
- **암호를 잊으면 누구도 열 수 없습니다.** 서버가 없어 되돌릴 방법이 없고, 이 화면에서 가장 중요한 경고입니다.
- 확장자는 `.smbk`, 평문 백업은 예전처럼 `.db`. **암호가 걸렸는지는 확장자가 아니라 머리의 표식으로 판별**합니다(`isEncryptedBackup`) — 이름은 얼마든지 바뀝니다. **평문 백업도 계속 읽습니다.**
- 암호를 비워 두면 예전처럼 평문 `.db`가 나옵니다. 이미 그렇게 만들어 둔 백업이 있고, 또 만들지 못하게 막는 것은 사용자 대신 결정하는 일입니다.

**암호화가 지키지 못하는 것** — 기기 안의 DB는 여전히 평문입니다. IndexedDB에 그대로 있어 브라우저 프로필에 접근할 수 있으면 읽힙니다. PIN은 화면을 가리는 자물쇠일 뿐 데이터를 암호화하지 않고, AI 키도 localStorage에 평문입니다. **백업 암호화는 파일 하나만 지킵니다.**

### 4.6 백업과 복원의 범위

**백업 파일은 사용자별이 아니라 `.db` 파일 전체입니다**(`exportDatabaseBytes()` = `db.export()`). 그래서 복원은 **이 기기의 모든 사용자**를 파일에 든 사용자들로 바꿉니다 — 백업에 없던 사람은 가계부째 사라지고, 백업에 있던 사람은 **PIN 해시까지** 되살아나 그 계정으로 로그인할 수 있게 됩니다.

- 확인 문구에 **이 기기의 사용자 명단**을 넣어, 누구의 가계부가 사라지는지 이름으로 말합니다.
- 복원 뒤에는 **반드시 로그아웃**합니다. 세션은 메모리의 `currentUserId`를 들고 있는데 복원된 파일에 그 id가 없을 수 있고, 그러면 모든 조회가 `WHERE user_id = '없는 id'`가 되어 **0건**이 됩니다 — 데이터가 사라진 것처럼 보이는, 4.7·14.8과 같은 함정입니다.
- "한 사용자의 것만 다른 계정으로 옮기기"는 **지원하지 않습니다.** 필요해지면 내보낼 때 현재 사용자 행만 담고 가져올 때 `user_id`를 갈아 끼우는 별도 기능으로 만드세요. 전체 백업과 섞지 마세요.

### 4.7 열지 못했을 때 (데이터 손실 방지)

**빈 데이터베이스와 열지 못한 데이터베이스는 정반대의 뜻입니다.** 예전에는 둘을 같게 다뤄서, 읽기가 한 번 실패하면 앱이 조용히 빈 DB로 시작하고 → 사용자 0명이니 **첫 사용자 등록 화면**을 띄우고 → 거기서 뭔가를 입력하면 `finance.db` **같은 키에 덮어써서** 원본이 사라졌습니다. 읽기 실패 한 번이 곧 영구 손실이었습니다.

지금은 이렇게 동작합니다(`db/database.ts`).

- `idbRead()` 실패 → `DatabaseUnavailable`을 **던집니다.** 빈 DB로 내려가지 않습니다.
- 저장본은 있는데 `new SQL.Database(bytes)`가 실패(손상) → **원본 바이트를 `finance.db.unreadable` 키로 먼저 보관**한 뒤 던집니다. 열리지 않는다고 쓸모없는 것이 아닙니다 — 그 바이트가 사용자 가계부의 유일한 사본입니다.
- `persist()`는 `openFailure`가 설정돼 있으면 **아무것도 쓰지 않습니다.** 읽지 못한 것 위에 쓰지 않는다는 규칙을 마지막으로 한 번 더 지킵니다.
- 저장본이 **아예 없을 때만** 새 DB로 시작합니다. 그것이 진짜 첫 실행입니다.
- `FinanceContext`가 `dbError`로 내보내고, `App.tsx`의 `AppGuard`가 **전용 화면**을 띄웁니다 — 주소·포트를 확인하라는 안내와 `다시 시도`뿐이고, **쓰는 버튼은 하나도 없습니다.**

**실패를 삼키지 마세요.** 화면이 "0건"으로 보이면 사용자는 데이터가 사라졌다고 믿습니다. 실제로 그렇게 신고를 받았습니다(14.8).

### 4.8 저장

`run()`/`runBatch()`는 쓰기 후 `scheduleSave()`로 IndexedDB 저장을 합칩니다(연속 쓰기를 한 번의 왕복으로). 개별 함수에서 `persist()`를 부를 필요가 없습니다.

---

## 5. 인증과 사용자

- **최초 실행 시 사용자 0명** → 3단계 등록 화면(이름·연락처 → PIN → 확인).
- PIN은 **PBKDF2-HMAC-SHA256, 310,000회, 16바이트 랜덤 솔트**(`pinCrypto.ts`). 평문은 저장하지 않습니다.
- 로그인은 사용자 선택 → PIN 6자리. 사용자별로 **가계부가 완전히 분리**됩니다.
- 설정 메뉴: `연동된 카드 및 계좌관리` / **`사용자 관리`** / `AI 등록` / `스마트폰 앱 설치`. **비밀번호 변경은 설정 메뉴에 없습니다** — 사용자 관리 안에 있습니다. 한 사람의 이름·연락처·비밀번호를 한자리에서 다루는 편이 맞고, 같은 일을 두 곳에 두면 한쪽만 고쳐집니다.
- **사용자 관리**(`UserManageModal`)에서 추가·**수정**·삭제를 합니다. 수정은 등록 때 받는 것을 전부 바꿀 수 있습니다 — `이름`, `연락처`, `간편 비밀번호`. **`id`만 고정**이고, 가계부 전체가 그 값에 매달려 있기 때문입니다. 비밀번호 칸을 비워 두면 그대로 둡니다.
- **이름은 유일해야 합니다.** 로그인 화면에서 사람을 고르는 값이기 때문입니다. `repository.isNameTaken(name, exceptId)`가 최종 판정이고, 수정 화면은 **PIN을 받기 전에** 먼저 확인해 알려 줍니다 — 6자리를 다 누른 뒤에 "이미 쓰는 이름"이라고 하면 헛수고입니다.
- **비밀번호는 글자로 받지 않습니다.** 수정 화면의 `간편비밀번호 등록·변경` 버튼이 `PinSetupModal`을 띄우고, 거기서 키패드로 두 번 눌러 확인합니다. 이름·연락처와 **따로 저장**되므로 그 화면에서 바꾸면 `[변경 확인]`을 누르지 않아도 적용됩니다(`savePinHash(대상id, …)`가 세션이 아니라 넘긴 id에 씁니다).
- **수정은 두 단계입니다**: `입력` → `[변경 확인]` → `확인`(내 PIN). 확인 단계에서는 **입력 항목을 감추고** 무엇이 어떻게 바뀌는지(`이름 홍길동 → 일지매`)만 보여 줍니다 — 그 자리는 "이대로 저장할까"만 묻는 자리이고, 거기서 비밀번호를 바꾸러 나가는 길이 보이면 무엇을 확인하는 중인지 흐려집니다. 뒤로가기(`←`)는 **한 단계씩** 돌아갑니다(확인 → 입력 → 목록). 목록으로 튕기면 입력한 것이 사라집니다.
- **이름·연락처가 그대로면 `[변경 확인]`은 눌리지 않습니다.** 비밀번호는 이 판정에 넣지 않습니다 — 그쪽은 전용 화면에서 이미 저장됐으니 확인할 것이 남지 않습니다.
- **수정도 삭제와 같이 로그인한 사용자의 PIN을 받습니다**(`editUser`, `resetPin`). 대상 사용자는 그 자리에 없고, 남의 이름이나 비밀번호를 바꾸는 일은 삭제만큼 무거운 일입니다. `resetPin(targetId, newPin, authorizingPin)` 하나가 자기 것과 남의 것을 모두 처리합니다 — 자기 비밀번호를 바꿀 때 확인받는 "현재 비밀번호"가 곧 로그인한 사용자의 비밀번호라 같은 길입니다.
- **마지막 사용자도 지울 수 있습니다.** 사용자 0명은 이 앱이 정상으로 다루는 상태이고(첫 실행 등록 화면), 막을 기술적 이유가 없습니다. 대신 삭제 화면이 **"마지막 사용자입니다. 이 기기의 가계부가 전부 사라지고 첫 사용자 등록부터 다시 시작합니다"**라고 분명히 말합니다.
- **사용자를 지우면 그 사람의 것이 전부 지워집니다** — `user_id`를 가진 7개 테이블(`accounts`·`transactions`·`budgets`·`budget_configs`·`ai_analyses`·`category_rules`·`custom_categories`)과 `users` 행이 **한 번의 배치**로, 그리고 **그 사용자의 AI 키**(11.1)까지. 새 테이블에 `user_id`를 넣으면 `repository.deleteUser`에도 반드시 추가하세요.
- localStorage에 남는 것: 기억된 명세서 형식, 숨긴 알림. 둘 다 가계부 데이터가 아니고 사용자 구분이 없습니다.
- 카카오·토스·PASS·네이버 같은 외부 인증은 **개인 개발자가 쓸 수 없습니다**(사업자·심사 필요). PIN이 대안입니다.

---

## 6. 카테고리 체계

### 6.1 목록 (`constants/categories.ts` — 단일 출처)

```
식비, 카페/간식, 주거, 통신, 구독/미디어, 교통, 쇼핑, 문화/여가,
생활, 의료, 보험, 대출, 기타 금융, 저축, 카드대금, 급여, 기타수입, 기타지출
```

**`저축`과 `카드대금`은 카테고리 예산에서 빠집니다**(`BUDGET_EXCLUDED_CATEGORIES`). 둘 다 다른 자리에서 이미 셈해지는 돈입니다 — 카드대금은 그 카드의 명세서로, 저축은 예산 화면의 `목표 저축액`으로. 가용 변동비가 `수입 − 고정비 − 저축`이라 저축에 또 예산을 주면 없는 돈을 배분하게 됩니다.

- `CategoryType = BuiltInCategory | (string & {})` — 사용자가 직접 만든 이름도 값이 됩니다.
- 사용자 카테고리는 `custom_categories`에 **사용자별로** 저장되고, 기본 목록 뒤에 붙습니다.
- 카테고리를 쓰는 모든 화면은 `useFinance().categories` 하나만 봅니다. 컴포넌트마다 목록을 복제하지 마세요.
- 선택 UI는 항상 `CategorySelect` 컴포넌트를 씁니다(`+ 직접 입력` 포함).
- 아이콘/색상: `TransactionItem.tsx`의 `getCategoryIcon`/`getCategoryBg`, `AnalyticsDashboardView.tsx`의 `CATEGORY_COLORS`. 셋 다 **default 분기**가 있어 새 카테고리도 깨지지 않습니다.

### 6.2 판정 우선순위 (절대 바꾸지 말 것)

```
1) 사용자 규칙 (category_rules, source=USER)
2) 앱 자체 판별 (카드대금 → 보험/대출/기타 금융)   ※ 지출 건에만 적용
3) AI 규칙 (category_rules, source=AI)
4) AI 자동 분류 결과 / 가져오기 시 키워드 추정
```

`resolveCategory(rules, merchant, accountId, isIncome)`가 1~3을 담당합니다. **입금 건에는 자체 판별을 적용하지 않습니다** — `예금이자`는 지출이면 대출 관련이지만 입금이면 수입이고, 방향만이 그 둘을 가릅니다.

### 6.3 자체 판별 규칙

- **카드대금**: `CARD_ISSUERS`(KB·국민·신한·삼성·현대·롯데·우리·하나·BC·비씨·NH·농협·씨티·카카오·토스·IBK·기업·우체국·수협·새마을·SC·케이뱅크와 지방은행) 중 하나가 **바로 뒤에 "카드"를 달고** 나오면 카드 결제대금으로 봅니다. `KB카드`, `우리카드출금`, `삼성카드결재`, `롯데카드1234`, `1234 신한카드` 모두 해당. 카드사명이 없는 `체크카드출금`은 제외됩니다.
- **보험 / 대출 / 저축 / 기타 금융**: `FINANCE_KEYWORDS`를 이 순서로 검사합니다(더 좁은 것이 먼저).
  → **`저축`이 `기타 금융`보다 먼저**입니다. `적금`·`예금`·`저축`·`청약`·`ISA`·`CMA`·`IRP`는 모아 두는 돈이라 `저축`으로, `펀드`·`연금`·`증권`·`신탁`·`투자`는 굴리는 돈이라 `기타 금융`에 남습니다.
  → v14가 과거 내역도 같은 자리로 옮깁니다 — **지출만**입니다. `예금이자`는 나가면 금융이지만 들어오면 수입이고, 방향만이 그 둘을 가릅니다(6.2). `category_rules`는 건드리지 않습니다: 사람이 "적금은 기타 금융"이라고 정해 둔 규칙을 앱이 뒤집을 이유가 없습니다(6.4).
- **주거/통신, 생활/의료**: `HOUSING_KEYWORDS`, `LIVING_KEYWORDS`. 이 표들은 **가져오기 추정과 마이그레이션 이관에 같이 쓰입니다** — 그래서 과거 데이터가 "새로 가져왔다면 갔을 자리"로 갑니다. 새 분리를 할 때도 같은 방식을 쓰세요(`CATEGORY_SPLITS`).

### 6.4 카테고리 규칙 (`category_rules`)

- `계좌 / 패턴 / 카테고리 / 등록구분(USER|AI)`.
- 패턴은 **포함 매칭**입니다. `*`는 임의 문자열, 공백 무시, 대소문자 무시. `코웨이렌탈*` → `코웨이렌탈08`, `코웨이렌탈09`.
- 우선순위: `USER` > 더 긴 패턴 > 최근 수정(`pickRule`).
- **규칙은 카테고리만 담습니다.** 고정비/변동비는 규칙에 없고, 규칙은 **앞으로 들어올** 내역에만 걸립니다. 이미 등록된 내역과 고정비를 함께 옮기려면 9.6을 쓰세요.
- 거래 내역 수정에서 카테고리를 바꾸면 규칙 등록 체크박스가 켜지고, 패턴은 끝자리 숫자를 떼어 제안합니다(`suggestPattern`).
- AI 자동 분류 결과도 `source=AI`로 적립되고, 사용자가 `사용자로` 버튼으로 승격할 수 있습니다.
- **AI 규칙이 USER 규칙을 덮어쓰지 못합니다**(`saveCategoryRule`에서 차단).
- `카테고리 관리` 화면: 규칙 목록(각 행에 체크박스), `전체 선택`, `새 규칙 등록`, **`카테고리 일괄 적용`**(선택한 규칙만 그 계좌 전체 내역에 재적용, 규칙별 적용 건수를 요약 팝업으로 보고), 하단에 `내가 추가한 카테고리` 관리.

---

## 7. 명세서 가져오기 (가장 까다로운 부분)

### 7.1 흐름

```
PICK(계좌·파일 선택) → MAP(열 지정·미리보기) → REVIEW(중복 확인) → DONE(결과)
```

**PICK의 계좌는 어디서 열었느냐로 정해집니다.** 카드·계좌 메인의 `엑셀·CSV 가져오기`는 첫 계좌로, **카드/계좌 내역 화면의 `엑셀·CSV` 버튼은 그 계좌로** 시작합니다(`defaultAccountId`). 사용자가 모달 안에서 바꾼 선택은 열려 있는 동안 그대로 둡니다. 14.7을 함께 보세요.

### 7.2 파일 해독

`decodeFile`: UTF-8 BOM → UTF-8 → euc-kr/windows-949 순서로 시도합니다. 한국 은행 파일은 대부분 euc-kr입니다.

**`.xls`는 세 가지 서로 다른 것에 붙는 이름입니다.** 진짜 BIFF 워크북, HTML 표, 그리고 CSV·TSV 텍스트. 한국 금융기관이 셋 다 `.xls`로 내려줍니다. 그래서 `loadStatementFile`은 **확장자가 아니라 바이트로 판별**합니다.

- `D0 CF 11 E0`(OLE2) 또는 `PK\x03\x04`(ZIP) → 진짜 워크북. `import("xlsx")`로 **동적 로드**(별도 청크 ≈500KB), `cellDates: true`, `sheet_to_csv({ blankrows: false, rawNumbers: false })`.
- 그 외 → 먼저 `decodeFile`로 **직접 해독**합니다. HTML이면(`<html`·`<table`·`<!doctype html`) 해독한 **문자열**을 `XLSX.read(text, { type: "string" })`에 넘겨 표 구조만 얻고, 아니면 텍스트 그대로 파싱합니다.
- **HTML은 `<table>`마다 시트가 하나씩 생깁니다.** 명세서는 섹션마다 표를 따로 쓰므로, 엑셀에서 시트 1개로 보이는 파일이 여기서는 11개로 잡힙니다. 정상입니다. 그러니 **워크북 경로와 HTML 경로는 시트 선택 로직을 공유해야 합니다** — 한쪽만 첫 시트를 쓰도록 두면 사용자가 시트를 골라도 아무 변화가 없습니다(실제로 그런 버그가 있었습니다).
- **텍스트 파일을 워크북 리더에 넘기면 한글이 깨집니다.** SheetJS가 바이트를 레거시 코드페이지로 읽기 때문입니다. 같은 내용인데 csv는 되고 xls는 안 되던 원인이 이것이었습니다.
- **무엇을 읽었는지 화면에 밝힙니다.** 시트가 둘 이상인 파일에서는 항목 확인 단계 제목에 `[시트이름]` 또는 `[전체 시트 N개]`가 붙고, 시트 선택 상자 위에 `전체 시트` 배지가, 아래에 한 줄 설명이 나옵니다. 삼성처럼 한 명세서가 시트 둘로 나뉜 경우 **합쳐 읽은 것인지 한 시트만 읽은 것인지**가 건수만 봐서는 구분되지 않기 때문입니다.
- 어느 시트도 거래 표를 갖지 않으면 **모든 시트를 이어 붙여 한 문서로** 읽습니다. 섹션의 머리글과 데이터가 서로 다른 표로 나뉘는 경우가 있기 때문입니다. 사용자도 시트 선택에서 `전체 시트 합쳐서 읽기`를 고를 수 있습니다(`sheetName === ""`).
- **시트 선택은 내용 기준**입니다(명세서는 표지·요약 시트로 시작하고, 시트가 10개 넘는 파일도 있습니다). 각 시트를 CSV로 바꿔 `머리글이 거래 표 같은가(힌트 2개 이상) + 읽히는 줄이 있는가` → `읽히는 줄 수` → `힌트 점수` 순으로 고릅니다. **줄 수만으로 고르면 안 됩니다** — 표지에 날짜와 숫자가 한 줄만 있어도 거래가 한 건인 진짜 시트와 동점이 되고, 먼저 나온 표지가 이깁니다. 사용자가 고른 시트가 있으면 언제나 그것이 우선합니다.

### 7.3 표 인식

- **거래 표는 파일의 첫 표가 아닐 수 있습니다.** 신한 명세서는 결제 계좌, 이용금액 요약, 포인트 잔액, 할인 내역이 각각 표 모양으로 앞에 오고, 진짜 표는 파일 중반의 `3. 카드사용내역` 아래에 있습니다. 앞쪽 블록들의 머리글도 `이용 금액`, `할인 금액`처럼 거래 표와 비슷하게 생겼습니다.
- 그래서 `findTable`은 **머리글 후보를 전부 시도하고, 그 아래 줄들이 실제로 "날짜 + 금액"으로 읽히는 개수가 가장 많은 후보를 고릅니다.** 표의 범위는 **다음 섹션이 시작되는 줄에서 끊습니다** — `4.장기카드대출 이용내역`처럼 번호가 붙은 제목 줄이거나, 거래로 읽히지 않으면서 머리글 단어가 2개 이상인 줄입니다. 끊지 않으면 뒤 섹션의 머리글과 `내역이 없습니다` 줄이 전부 "읽지 못한 줄"로 보고됩니다. 힌트 단어 점수는 동점일 때의 보조 기준입니다. 읽히는 줄이 하나도 없으면(= 내역이 비어 있는 명세서) 가장 거래 표다운 머리글을 골라 사용자가 고칠 수 있게 합니다. 줄 수 제한은 없습니다.
- **같은 표가 여러 번 나오면 이어 붙입니다.** 삼성은 한 명세서를 시트 둘로 나눕니다 — `할부` 시트와 `일부결제금액이월약정(리볼빙) 일시불` 시트. 예전에는 둘 중 긴 쪽만 읽혀 **같은 파일을 두 번 등록**해야 했습니다. 이제 `findTable`이 고른 표와 **열 구성이 같은 블록**을 문서 순서대로 함께 가져옵니다(`sameShape`). 짧은 쪽이 `적립금액`·`입금후잔액`에서 끝나므로 **짧은 쪽 길이만큼만 비교**하고, 이미 가져온 블록과 **겹치는 범위는 제외**해 같은 줄이 두 번 들어오지 않게 합니다. 열 구성이 다른 신한의 `취소매출`이나 롯데의 요약표는 합쳐지지 않습니다.
- **두 줄 머리글 병합**: 머리글 다음 줄이 날짜도 금액도 아니면 하위 머리글로 보고, **병합된 제목을 아래 빈 칸으로 이어 붙여** 합칩니다. KB의 `이번달 결제금액`(제목) / `회차·원금·수수료(이자)`(이름), 신한의 `이번달 납부금액` / `원금·수수료(이자)`가 이 경우입니다. 이걸 안 하면 `회차` 열이 금액으로 잡혀 **할부 건만 인식되고 나머지가 전부 사라집니다.**

### 7.4 열 매칭 (3단계, 이 순서)

1. **기억된 형식** — `statementFormats.ts`가 머리글 서명(`공백 제거·소문자·| 연결`)으로 localStorage에 보관합니다. 사용자가 MAP 단계를 넘어가면(= 사람이 확인함) 저장합니다. 다음 달 같은 파일은 호출 없이 즉시 적용됩니다.
   **단, 그 기억이 이 파일을 규칙보다 못 읽으면 물러섭니다**(`chooseMapping`). 인식 규칙을 고치기 전에 저장된 지정이 남아 있으면 고친 내용을 영원히 덮어씁니다 — 롯데 `입금하실 금액`을 수입으로 읽던 지정이 저장돼 있어, 수정 후에도 계속 1건만 인식되던 일이 실제로 있었습니다. 기억이 `scoreMapping().ok`이거나 새 인식보다 많이 읽으면 유지하고, 아니면 다시 인식한 뒤 화면에 그 사실을 알립니다.
   **기억이 모르는 열은 새 인식으로 채웁니다**(`withNewColumns`). 저장된 형식은 **저장될 당시에 있던 열에 대해서만** 말할 수 있습니다 — `회차`·`수수료`는 한참 뒤에 생겼고, 그 전에 저장된 기억에는 그 항목이 `-1`("이 파일엔 그런 열이 없다")도 아니고 **아예 없습니다**. 그대로 두면 지난해 코드처럼 읽어, 회차가 메모에 실리지 않아 **다음 달 할부가 지난달 것과 같은 중복 키**를 갖습니다(8월 등록 후 7월을 넣으면 할부 1건이 "이미 등록됨"이 되던 원인). 값이 숫자가 아닌 항목만 채우므로, **사람이 "없다"고 확인한 `-1`은 건드리지 않습니다.**
   → **새 열을 `ColumnMapping`에 추가할 때마다** 이 규칙 덕에 기존 사용자의 기억이 자동으로 따라옵니다. 열을 추가하고 이 동작을 확인하지 않으면 같은 사고가 반복됩니다.
2. **규칙 판별** — `autoDetectMapping(headers, rows)`. 이름으로 찾고, 그 결과가 **날짜 있는 행의 60% 이상을 읽지 못하면** 실제 데이터에서 다시 찾습니다(`detectFromRows`).
3. **AI 판별** — 위가 실패하고 키가 등록되어 있을 때만. `detectStatementColumns(headers, sampleRows)`에 **머리글과 표본 5행만** 보내고 열 번호만 받습니다. 파일 전체를 보내지 않으며, 규칙보다 잘 읽을 때만 채택합니다. 화면에 출처(`자동 지정` / `AI가 인식` / `이전에 확인한 형식`)를 표시하고 `AI로 다시 인식하기`를 제공합니다.

**이번 달 청구분 열 이름**: 카드사마다 `이번달 결제금액`(KB) · `이번달 납부금액`(신한) · `이번 달 입금하실 금액`(롯데) · `원금`만 덩그러니(삼성) · `청구금액`으로 갈립니다. **`원금`은 후보 목록의 맨 뒤**에 둡니다 — 앞의 카드사들은 `… 결제금액 원금`처럼 더 긴 이름으로 먼저 걸리고, 삼성만 이 규칙으로 잡힙니다. 삼성에는 `이용금액`(구매 총액)이 따로 있어서, 그것을 금액으로 읽으면 16,000원 할부 건이 **160,550원**으로 들어옵니다. 모두 **지출** 열 후보입니다. 특히 롯데의 `입금하실`은 "입금"이 들어 있어 수입 열로 잡히기 쉬운데, 그러면 한 달 치 사용액이 통째로 수입이 됩니다 — `입금하실`은 입금 후보에서 제외합니다.
**수수료 열**(`수수료`·`이자`)은 따로 매칭하고, **청구액은 `원금 + 수수료`입니다.** 수수료는 원금 대신이 아니라 원금과 **함께** 청구됩니다. 원금이 비어 있으면 수수료가 그 줄의 금액이 됩니다 — 롯데의 `크레딧케어 990`처럼 수수료만 있는 줄도 한 건입니다.
**금액이 될 수 없는 열**: 잔액, 한도, 누계, 회차, 개월, 건수, 포인트, 마일리지, 번호, **적립**, **혜택**. (`적립예정 포인트`가 "적립"에 걸려 입금으로 읽히던 사고가 있었습니다.)
**메모 열**은 `매출구분` → `구분` → `할부개월` 순입니다. 우리카드처럼 `구분`이 든 열이 둘일 때(`카드구분`=신용/본인, `매출구분`=국내할부·일시불·차감·카드소계) **뜻이 있는 쪽**을 잡아야 합니다. `할부`/`일시불` 값이 할부 판정에 쓰이고, `카드소계` 같은 값이 합계 판정의 근거가 됩니다.

### 7.5 값 해석

- `normaliseDate`: `2026-09-05`, `2026.9.5`, `20260905`, **`26.08.03`(두 자리 연도)**, `260803`, `09-05`(연도 없음) 순으로 시도하고, 월>12·일>31이면 그 해석을 버립니다. 두 자리 연도 미지원 때문에 명세서 전체가 `2026년 26월`로 들어간 사고가 있었습니다.
- `normaliseAmount`: 콤마·원·공백 제거, 음수 부호 인식.
- **청구 열의 음수는 차감입니다.** 우리카드는 `차감-[청구할인] …`을 `원금 −10,000`으로 청구합니다. 지출로 읽으면 그 달에 10,000원을 **더하게** 되고, 월 합계가 어떤 출금과도 맞지 않아 **카드대금 자동 연결이 깨집니다**(9.2는 정확히 같을 때만 연결). 그래서 음수면 `INCOME`으로 기록해 그 달에서 빠지게 합니다(§9.4의 "환불은 차감합니다"와 같은 취급). 입금 열의 음수는 반대로 `EXPENSE`입니다.
- **바로 윗줄을 고치는 줄**: KB는 무이자 할부의 이자를 `원금 25,947 / 수수료 838`로 청구한 뒤, **바로 아래 `무이자혜택금액` 줄에서 수수료 −838**을 돌려줍니다. 날짜가 없고 수수료가 음수인 줄은 **직전 줄의 금액에서 빼고**, 그 줄 자체는 내역이 되지 않습니다(`buildDrafts`의 `lastDraftRow` 인접 확인). 무이자가 아닌 건(`원금 17,321 / 수수료 46`)은 돌려주는 줄이 없으므로 수수료가 그대로 붙습니다.
  → **수수료를 원금에 상계해서 없애면 안 됩니다.** 그러면 무이자가 아닌 46원이 사라지고, 그것이 곧 **월 합계 불일치 → 카드대금 자동 연결 실패**가 됩니다(9.2는 금액이 **정확히** 같을 때만 연결합니다). 실제로 그렇게 어긋난 적이 있습니다.
  → `톡톡 교통/통신 할인 −870`처럼 **이용금액**만 음수인 줄은 이미 원금(`8,700 → 7,830`)에 반영돼 있으므로 따로 세지 않습니다.
- **합계 줄**: 대부분의 카드사는 합계 줄의 가맹점 칸을 비워 두므로 "가맹점이 없으면 내역이 아니다"로 걸러졌습니다. **삼성과 우리는 그 칸에 합계를 씁니다** — 결제월을 지정하면 날짜까지 얻어 이용 내역으로 등록됐습니다(삼성 `할부합계 185,200`, 우리 `소계(양승현) 814,623`·`청구합계-국민은행 357******155`).
  `isTotalLabel`은 **`합계`·`소계`·`총계`·`누계` 뒤에 한글이 이어지지 않으면** 합계로 봅니다(`/(합계|소계|총계|누계)(?![가-힣])/`). **가맹점 칸과 `구분` 칸 둘 다** 검사합니다 — 우리카드는 `매출구분`에 `카드소계`·`청구결제번호소계`라고 더 분명히 적습니다.
  → **위치로 판단하면 안 됩니다.** 카드사마다 다릅니다 — 삼성은 뒤(`일부결제금액이월약정(리볼빙) 일시불합계`), 우리는 앞이나 가운데(`소계(양승현)`, `청구합계-국민은행 …`). 공통점은 **그 말이 거기서 끝난다**는 것뿐이고, 다음 글자가 괄호·부호·숫자이거나 없습니다.
  → **"포함"으로 바꾸지 마세요.** `종합계좌이체`는 가운데에 `합계`가 든 **진짜 거래**입니다. 뒤에 한글이 오는지가 이 둘을 가르고, 포함으로 하면 그 거래가 조용히 사라집니다. 이용 내역 하나가 없어지는 쪽이 합계 하나가 끼는 쪽보다 나쁩니다.
- **날짜 없는 줄**: 연회비·이자처럼 이용일 없이 청구되는 항목이 있습니다. 가맹점 칸이 채워져 있고 금액이 있으면 **가져오기에서 지정한 결제월의 1일**로 등록합니다(`buildDrafts(table, mapping, { fallbackDate })`). 결제월을 지정하지 않으면 적용하지 않고, 가맹점 칸이 빈 `합계`·`소계`·안내 문구 줄은 이 규칙으로도 들어오지 않습니다.
- `guessCategory`: 자체 판별(카드대금/금융/주거·통신/생활·의료) → 키워드 표 → `기타지출`/`기타수입`.
- `guessExpenseType`: 고정비 키워드로 1차 추정(최종 판정은 AI 자동 분류에서).

### 7.6 중복 판정

- 키: `날짜 | 공백 제거한 내역명 | 금액 [| 할부 회차]`
- **가져오는 그 계좌 안에서만** 비교합니다. 전체 가계부에서 비교하다가 다른 카드의 같은 결제가 중복으로 잡혀 명세서 전체가 0건 등록되던 사고가 있었습니다.
- **할부**: 같은 구매가 매달 같은 날짜·가맹점·금액으로 반복 청구됩니다. **회차만이 달을 구분합니다.**
  - 회차는 대개 **별도 열**(`회차`)에 있고, 옆의 `할부`/`할부개월` 열은 총 개월수라 매달 같습니다. 그래서 `instalment` 열을 따로 매칭해 **회차를 메모에 실어** 넣습니다 — `4/10`(총개월수를 아는 경우), `9회차`(모르는 경우). 이 값이 중복 키의 일부가 됩니다.
  - **`결제 후 잔액 회차`는 남은 횟수**라서 후보에서 제외합니다.
  - 회차를 끝내 알 수 없고 `할부`라고만 되어 있으면 **중복 판정에서 제외**합니다. 대신 같은 파일을 두 번 올리면 중복 등록됩니다.
  - 이것을 놓치면 **다음 달 명세서의 할부 건이 지난달 것과 같은 건으로 걸러집니다.** 롯데 명세서에서 9건 인식 · 8건 등록이 되던 원인이었습니다.
- REVIEW 화면은 **모든 줄을 파일 순서대로** 보여주고 각 줄에 `추가`(초록) / `중복`(주황) 태그를 붙입니다. 덮어쓰기로 선택한 중복은 남색, 건너뛰기는 회색+취소선. `전체/추가/중복` 필터와 `모두 건너뛰기`/`모두 덮어쓰기`를 제공합니다.

### 7.7 카드 명세서 고유 항목

- **결제(청구) 년월**: `결제일·청구년월` 열이 있으면 행마다 읽고, 없으면 화면에서 한 번 지정합니다. `transactions.billing_month`에 저장됩니다.
- 그 기본값은 `guessBillingMonth(fileName, 내역들)`가 정합니다.
  1. **파일 이름에 적힌 연월이 우선**이고, **그 연월이 곧 결제월**입니다(더하지 않습니다). 카드사마다 적는 방식이 달라 네 가지를 **이 순서로** 읽습니다.

     | 차례 | 형태 | 예 | 결제월 |
     | --- | --- | --- | --- |
     | 1 | `YYYY년 M월` | **신한** `2026년9월 이용대금명세서(신한카드).xls` | 2026-09 |
     | 2 | `YYYYMM` (구분자 있어도 됨) | `신한카드_202603_명세서.csv`, `2026-03 카드내역.xlsx` | 2026-03 |
     | 3 | `YYMM` (정확히 4자리) | **롯데** `이용대금명세서_2608(신용.체크)_20260915091506.xls` | 2026-08 |
     | 4 | `YYYYMMDD` (정확히 8자리) | **KB** `이용대금명세서_20260903.xlsx`<br>**삼성** `samsungcard_20260826.xlsx` | 2026-09<br>2026-08 |

     - **`YYMM`**: 앞 두 자리가 `20`~`39`일 때만 연도로 봅니다. `2026`은 네 자리지만 `26`월이 없어 스스로 탈락합니다.
     - **`YYYYMMDD`의 `DD`는 카드사마다 다른 것을 가리킵니다** — KB는 발송일(9월 명세서를 9월 3일에 보냄), 삼성은 결제일. 어느 쪽이든 **앞의 연월이 결제월**이라는 점만 같으므로 일자는 `1`~`31`인지만 확인하고 버립니다. 그래서 이것이 **가장 약한 근거이고 맨 뒤**입니다 — `2608`·`202608`은 결제월을 대놓고 적은 것이라 한 이름에 둘 다 있으면 그쪽이 이깁니다.
     - **자릿수가 곧 안전장치입니다.** 4자리와 8자리만 보므로 `20260915091506`(14자리, **내려받은 일시**)은 어느 규칙에도 걸리지 않습니다. 이 값은 결제월과 아무 관계가 없습니다.
     - 이름에서 못 읽으면 **아무 것도 반환하지 않고** 2번으로 내려갑니다. 생성일시에서 억지로 달을 뽑지 않습니다.
  2. 이름에 없으면 **가장 늦은 이용월의 다음 달**입니다(카드는 쓴 다음 달에 청구됩니다). 이때 **할부 건은 빼고 셉니다** — 몇 달 전 구매일을 그대로 갖고 있어서, 그것을 기준으로 삼으면 명세서가 과거로 밀립니다. 할부밖에 없으면 그것으로라도 추측합니다.
- **잔액 조정 체크박스는 은행 계좌에만** 나옵니다. 카드는 잔액이 아니라 청구액입니다.

### 7.8 결제 문자 등록 (`services/smsParse.ts`)

명세서를 매달 내려받아 넣는 일은 번거롭고, 달이 끝나기 전에는 명세서가 없습니다. 문자는 그 틈을 메웁니다 — **즉시성**이 이 기능의 값 전부이고, 정확성은 명세서가 맡습니다.

**문자함을 앱이 읽을 수는 없습니다.** 브라우저에 그런 API 가 없고, Android 의 `READ_SMS` 는 네이티브 앱 권한(PWA 는 `앱 설치` 로 설치해도 브라우저 문맥)이며 iOS 에는 네이티브조차 없습니다. `Web OTP API` 는 이 도메인으로 보낸 특수 형식의 인증번호 한 개만 받을 수 있어 쓸 수 없습니다. 그래서:

넘기는 길이 셋이고 **클립보드가 실제로 가장 짧습니다.**

1. **`텍스트 복사` → [복사한 문자 담기]**(`navigator.clipboard.readText()`). 기본 경로입니다 — **삼성 메시지의 말풍선 길게 누르기 메뉴에는 공유가 없습니다.** `전달`(문자 앱 안에서만)과 `텍스트 복사`뿐이고 안드로이드 공유 시트를 띄우지 않습니다. 여러 건을 함께 복사하면 한 번에 담깁니다.
2. **붙여넣기** — 클립보드 권한이 막혔거나 https 가 아닐 때. 길이 끊기지 않게 항상 둡니다.
3. **공유**(매니페스트의 `share_target`, GET) — **앱을 설치했을 때만** 공유 목록에 나타나고, 매니페스트가 바뀌면 **다시 설치**해야 반영됩니다. 삼성 메시지에서는 `텍스트 선택` 후 선택 도구의 공유를 써야 합니다. 공유된 글은 주소 쿼리(`share_text`)로 들어오고 `App.tsx` 가 담은 뒤 **주소를 즉시 지웁니다** — 새로 고치면 또 담기고, 결제 문자가 주소창과 방문 기록에 남습니다.

- **기간 선택 칸은 두지 않습니다.** 무엇을 넘길지 고르는 행위가 곧 기간과 대상을 정하는 일입니다.

**파싱은 규칙으로 합니다. AI 를 쓰지 않습니다.** 문자에는 가맹점·금액·잔액이 담겨 있고 그것을 외부 서버로 보낼 이유가 없습니다. 한국 카드사 문자는 형식이 거의 고정돼 있어 규칙으로 읽히고, 그러면 **AI 키 없이도 동작하며 즉시 끝납니다**(7.4 가 규칙을 먼저 쓰는 것과 같은 이유). 못 읽은 칸은 **비워 두고**(17.1) 목록에서 사용자가 고칩니다.

- 금액: `누적`·`잔액`·`한도`·`적립` 뒤의 숫자는 거래액이 아닙니다. 그 말이 없으면 **처음 나온** 금액이 거래액입니다 — 카드사는 쓴 돈을 먼저 적습니다.
- 날짜: 연도가 없는 문자가 대부분입니다. 올해로 보되 **미래가 되면 지난해로** 봅니다(1월에 12월 문자를 넣는 경우).
- 방향: `취소`·`환불`이 `승인`보다 셉니다 — `승인취소`에는 두 말이 함께 있고 그 건은 돈이 돌아온 것입니다.
- 가맹점: 아는 것(머리말·사람 이름·날짜·시각·금액·카드사·결제 구분)을 모두 지우고 남는 가장 긴 조각. `급여` 같은 방향 표시도 빠지므로, **카테고리 추정은 가맹점이 아니라 문자 원문**으로 합니다.
- **금액을 못 읽으면 거래로 보지 않습니다** — 인증번호·이벤트 안내가 걸러지는 첫 관문입니다.
- **금액이 적힌 알림도 걸러야 합니다**(`looksLikeNotice`). 청구금액 통지는 **한 달치 금액**이라 거래로 들어오면 그 달 합계가 통째로 비틀립니다. 세 가지 분명한 신호만 봅니다 — **광고 표시**(`광고`·`무료수신거부`), **예정/예상**(`출금예정`·`결제예정`), **청구 통지**(`청구금액`·`이용대금`·`명세서발행`). 인증번호도 함께.
  → **`안내`라는 말로는 가릴 수 없습니다.** 실제 우리카드 승인 문자에 `● 우리카드 이용안내`가 들어 있습니다. 그것을 알림으로 보면 **진짜 거래가 사라집니다.**
- **상투구는 가맹점이 아닙니다**(`BOILERPLATE`). 카드사가 머리에 다는 `이용안내`·`확인된 발신번호`·`안내` 를 남겨 두면 진짜 가맹점보다 길어서 그쪽이 이름으로 뽑힙니다. **조각 전체가 그 말일 때만** 버립니다 — 부분 일치로 지우면 `안내상회` 같은 진짜 이름이 깎입니다. 앞뒤의 `● ✅ ※ ▶` 도 떼어 냅니다(안 떼면 `✅확인된 발신번호`가 목록과 글자로 달라 그대로 이름이 됩니다).
- **빠진 건수를 화면이 말합니다.** `receiveSmsText` 가 `{added, skipped, ignored}` 를 돌려주고, `ignored`(거래가 아닌 것)를 함께 보여 줍니다. 말하지 않으면 있어야 할 건이 없을 때 사용자가 알아차릴 방법이 없습니다.

**대기함**(`sms_inbox`)에 쌓아 두고, 사용자가 계좌를 고르고 확인한 뒤 등록합니다. 확인 전의 추정을 `transactions` 에 섞으면 합계가 흔들립니다. 문자가 카드사를 말하고 **그 카드사 계좌가 하나뿐일 때만** 미리 골라 둡니다(9.2 와 같은 기준).

**중복 판정이 이 기능의 핵심입니다.** 같은 거래가 나중에 명세서로 다시 들어오기 때문입니다.

- `duplicateKey`(7.6)는 **내역명이 같아야** 중복으로 봅니다. 문자와 명세서 사이에서는 이름이 거의 언제나 다릅니다 — 문자 `스타벅스 강남R점` / 명세서 `스타벅스강남알`.
- 그래서 **`findSimilarEntry`** 가 `같은 계좌 + 같은 날짜 + 같은 금액 + 같은 방향`을 더 센 근거로 봅니다. 이름까지 같으면 `EXACT`, 다르면 `LIKELY`.
  → **방향도 봐야 합니다.** 28,500원 결제와 28,500원 승인취소는 같은 날 같은 금액이지만 서로 다른 사건입니다.
- **문자 → 등록**: `EXACT`·`LIKELY` 모두 화면에 `중복`·`확인 필요`로 표시하고, 고르면 그 줄을 **덮어씁니다**. `LIKELY` 는 같은 날 같은 금액을 다른 곳에서 쓴 것일 수도 있어 **사용자가 정합니다**(17.2).
- **명세서 → 가져오기**: `LIKELY` 이면서 그 줄이 **`origin === "SMS"`** 일 때만 같은 건으로 보고 기본값을 **덮어쓰기**로 둡니다. 명세서가 더 정확한 기록이기 때문입니다. 사람이 넣은 줄이나 다른 명세서 줄은 건드리지 않습니다.
  → 이것을 빼먹으면 같은 결제가 두 줄이 되어 **그 달 합계가 두 배**로 잡히고, 카드대금 자동 연결(9.2)까지 어긋납니다.

---

## 8. 잔액과 기준일시

- 잔액은 **금액 + 기준일시(`0000년00월00일 00시`) + 출처(`USER`/`AUTO`)** 세 가지가 한 벌입니다. 계좌 등록 시에도, 수정 시에도 함께 받습니다.
- **기준일시 이전 거래는 잔액을 움직이지 않습니다.** 이미 그 금액에 반영돼 있기 때문입니다(`services/balance.ts`의 `movesBalance`).
- 통장: 입금 +, 출금 −. 카드: 사용 +, 환불 −.
- 가져오기 시 `현재 잔액 수정`을 체크하면 기준일시 이후 항목만 합산해 조정하고, 새 기준일시는 반영된 항목 중 가장 늦은 거래 시점, 출처는 `AUTO`가 됩니다.
- 카드의 **청구 예정액은 사용자가 입력하지 않습니다**(9.3 참조). 카드 행에는 수정 버튼이 없습니다.

---

## 9. 카드와 결제 계좌

### 9.1 연결 구조

- 카드에는 `payment_account_id`(등록된 계좌) 또는 `payment_account_label`(직접 입력)이 있습니다. 등록 화면과 수정 화면 양쪽에 있습니다.
- 은행의 카드대금 출금 건에는 `linked_account_id`(어느 카드) + `billing_month`(어느 결제월)가 저장됩니다.
- **연결은 언제나 "계좌의 출금 ↔ 카드의 명세서"입니다.** 카드 자기 내역은 **어떤 경우에도 명세서와 연결되지 않습니다** — 명세서의 한 줄은 그 명세서의 일부일 뿐, 명세서를 대표하지 않습니다. 우리카드가 청구할인을 `차감-[청구할인] 청호나이스 우리카드II …`로 적는 바람에 카드사 이름이 걸려 카드대금으로 분류되고, 자기 명세서를 가리키는 바로가기까지 붙은 적이 있습니다.
  → `settlesFromBank(accountId, accounts)`가 이 규칙을 담고, **판단하는 자리마다** 세워져 있습니다: `planCardLinks`(자동 연결), `FinanceContext.withRule`(가져오기·직접 추가), 거래 수정 화면의 자동 연결, 계좌 내역 목록의 `N월 명세서 ›` 바로가기. **하나라도 빠뜨리면 그 경로로 되살아납니다.**
  → `pendingBill`은 `linked_account_id`가 자기 자신인 옛 데이터를 정산으로 치지 않습니다.

### 9.2 어느 카드인지 가리는 법 (`matchCardForBill`)

```
1) 내역명의 카드사로 후보를 좁힌다 (없으면 전체 카드)
2) 후보 중 "결제월 합계 == 출금액"인 카드·결제월을 찾는다
3) 그런 후보가 둘 이상이면 연결하지 않는다 (같은 금액을 청구한 다른 카드일 수 있음)
4) 금액으로 정해지지 않고 해당 카드사 카드가 한 장뿐이면 그 카드
5) 다른 출금이 이미 가져간 결제월은 후보에서 제외 (1 출금 ↔ 1 명세서)
```

**카드사 이름만으로 연결하면 안 됩니다.** 같은 카드사 카드가 여러 장일 수 있습니다.

### 9.3 자동 연결·해지 (`planCardLinks`)

판단은 `services/cardLink.ts`의 **순수 함수** `planCardLinks(accounts, transactions, touchedAccountIds, 카드대금)`가 하고, `FinanceContext.reconcileCardBills`는 그 결과를 저장만 합니다(§17.5). 가져오기·삭제 시점에 실행됩니다.

- 결제월 합계와 같은 금액의 카드대금 출금이 있으면 자동으로 연결하고 **결제월까지 기록**합니다.
- **연결은 양쪽 어디서 완성돼도 됩니다.** 카드 명세서가 들어와 합계가 출금과 같아지는 경우와, 그 대금을 낸 **은행 거래내역이 나중에 들어오는** 경우 둘 다입니다. `touchedAccountIds`에 **그 카드** 또는 **그 카드의 결제 계좌**가 들어 있으면 검토합니다.
  → 예전에는 카드 쪽만 봤습니다. 그래서 카드를 먼저 등록하고 은행을 나중에 등록하면 **금액이 정확히 같아도 영영 연결되지 않았습니다.** 실제로 겪은 일입니다.
- **결제 계좌를 등록하지 않은 카드**는 출금 내역에 카드사 이름이 적혀 있고 그 카드사 카드가 **한 장뿐일 때만** 연결합니다(`matchCardAccount`와 같은 기준). 금액은 여전히 정확히 같아야 합니다.
- **해지는 카드 쪽 내역이 바뀌었을 때만** 합니다(`touched.has(card.id)`). 그 결제월 내역이 모두 삭제되면 출금이 어떤 달과도 맞지 않게 되므로 **연결과 결제월을 함께** 해지하고, 다른 달의 연결은 그대로 둡니다. 은행 거래내역을 넣었다는 이유로 해지하면, 명세서를 아직 등록하지 않은 카드에 **사람이 직접 걸어 둔 연결**이 풀립니다.
- **이미 다른 카드에 연결된 출금은 빼앗지 않습니다.** 한 계좌에서 결제되는 두 카드가 같은 달에 같은 금액을 청구할 수 있고, 나중에 검사한 카드가 이기면 엉뚱한 카드의 청구서가 됩니다.
- 연결을 **기록**하는 것이 핵심입니다. 화면에서 매번 다시 계산하면 자동 연결과 수동 연결이 서로 다르게 보입니다(실제로 그런 버그가 있었습니다).
- `addTransaction`·`updateTransaction(s)`는 이 함수를 부르지 않습니다. 가져오기 경로의 `withRule`이 새 카드대금 건에 대해 `matchCardForBill`로 한 번 맞춰 봅니다.

### 9.4 청구 예정액 (`pendingBill`)

사용자가 입력하는 값이 아니라 계산합니다.

1. 결제 확인이 된 경우 → **마지막으로 정산된 결제월 이후** 등록된 이용분 (`8월 결제 이후 이용분`)
2. 미정산 명세서가 두 달 이상 쌓인 경우 → **가장 최근 명세서** 기준 (`9월 명세서 기준`)
3. 명세서 정보가 없는 경우 → **이번 달 1일부터** 이용분 (`9월 1일부터 이용분`)

목록의 건수도 같은 기준입니다. 환불은 차감합니다.

**낸 대금은 낸 것으로 보여야 합니다.** `pendingBill`은 `settledMonth`·`settledAmount`로 **마지막으로 출금이 정산한 명세서**를 함께 돌려줍니다.

- 정산이 끝났고 그 뒤로 쓴 것이 없으면(청구예정 0원) 카드 행은 **낸 금액 + `N월 결재완료`**(초록)를 보여 줍니다. `0원 이번 달 청구예정`은 정산된 카드가 아니라 **쓰지 않은 카드**처럼 읽힙니다.
- 낸 뒤에 또 쓴 것이 있으면 그 금액은 아직 낼 돈이므로 `이번 달 청구예정`이 맞습니다.
- 건수 배지는 "결제 이후 이용분"의 건수라, 정산된 카드에서는 0건이 되어 낸 금액 옆에 붙으면 헷갈립니다 — 그때는 감추고 설명 줄이 대신 말합니다.
- 상단 `카드 결제 예정액` 합계는 **아직 낼 돈**만 더합니다. 정산된 카드가 0원으로 들어가는 것이 맞습니다.
- 카드 행의 **금액은 한 줄**이어야 합니다. `1,202,440원`이 숫자와 `원`으로 갈려 두 줄이 된 적이 있습니다 — 건수는 이름 옆 태그가 아니라 **설명 줄에 `이번 달 청구예정 (5건)`** 으로 붙여 금액이 쓸 폭을 넓히고, 세 줄 모두 `whitespace-nowrap`입니다.

### 9.5 화면

- **카드의 합계는 어디서든 `지출 − 차감·환불`입니다.** 카드는 한 덩어리 금액을 청구하고 청구할인·환불은 그 금액을 **줄이는** 것이라, 지출만 더하면 명세서의 소계와 어긋납니다(우리카드에서 34,000원 차이가 났습니다). 차감이 있으면 `이용 … − 차감·환불 …`을 작은 글씨로 함께 보여 줍니다.
  → **세 곳이 같은 값을 말해야 합니다**: 카드 내역 화면의 `이용 합계`, 그 화면의 `선택 N건 합계`, 그리고 카드대금 바로가기로 열리는 **카드 이용 내역(`CardUsageModal`)의 `이용 합계`**. 마지막 것이 이용액만 더하는 바람에, 바로 옆 칸이 `결제액과 일치 0원`이라고 말하는데 합계는 690,141원으로 찍혀 두 칸이 서로 다른 말을 한 적이 있습니다. 새 화면을 만들 때도 이 값을 쓰세요.
  → **계좌는 다릅니다.** 거기서 수입은 들어온 돈이지 줄어든 청구액이 아니므로 `잔액·지출 합계·수입 합계`를 따로 쓰고, 선택 합계도 더합니다.
- 카드 내역 화면에는 **잔액·수입 합계가 없습니다.**
- 구분 필터는 카드에서 `전체·고정비·변동비`(수입 없음), 계좌에서 `전체·고정비·변동비·수입`.
- 카드 전용 필터: `전체·카드대출·일시불·할부`. 카드대출은 카테고리가 `대출`인 건, 할부는 메모의 할부 표기로 판정합니다.
- 조회 기준: 카드는 `결제월 기준` / `이용일자 기준`을 고를 수 있습니다. 할부는 한 번 쓰고 여러 달 청구되므로 두 답이 다릅니다.
- 계좌 목록에서 카드대금 건의 금액 아래에 `8월 명세서 ›` 바로가기가 나오고, 누르면 그 달의 카드 이용 내역이 열립니다.

---

## 9.6 한 건의 수정을 같은 내역명 전체로 (`services/spread.ts`)

카드 명세서는 같은 가맹점을 달마다 되풀이합니다. 그래서 한 줄에 대해 내린 판단은 거의 언제나 **나머지 전부에 대한 판단**이고, 그것을 열두 번 다시 입력하게 하는 것은 앱이 그 사실을 못 알아챈 것입니다. 거래 수정 화면에서 **설명**과 **분류**를 같은 방식으로 넓힐 수 있습니다.

판단은 전부 `services/spread.ts`의 **순수 함수**이고 화면은 결과를 저장만 합니다(§17.5). 어느 함수도 직접 쓰지 않고, **무엇이 바뀔지 건수로 먼저 보여 준 뒤** 사람이 누릅니다.

**공통 규칙** — 같은 내역명은 **같은 계좌 안에서** 공백·대소문자를 무시하고 비교합니다(`sameMerchant`). 카드와 통장에 같은 이름이 찍혀도 서로 다른 결제이고, 수정하는 화면이 그 계좌의 내역이기 때문입니다. 선택지는 **수정 중이고 같은 내역명이 2건 이상일 때만** 나타납니다.

### 설명 (`planNote` · `noteReach`)

명세서의 내역명은 `삼성전자(주)`처럼 무엇을 샀는지 말해 주지 않습니다. `note` 칸이 그것을 받습니다(4.4 — `memo`와 주인이 다릅니다).

- **범위**: `이 건만` / `같은 내역명 N건 모두`.
- **이미 설명이 적힌 건은 기본적으로 건너뜁니다.** 사람이 적어 둔 한 줄이 그 건에 대해 알려진 가장 구체적인 사실입니다. 몇 건이 그런지 먼저 보여 주고 `그대로 두기`/`덮어쓰기`를 고르게 합니다(§17.2·§17.3).
- **가져오기에서 덮어쓰기를 골라도 `note`는 남습니다.** 명세서는 그 칸에 대해 할 말이 없습니다. 같은 이유로 수동으로 걸어 둔 `linkedAccountId`와 기존 `billingMonth`도 초안이 값을 갖지 않으면 보존합니다.

### 분류 — 카테고리와 고정비 (`planClassification` · `classificationReach`)

**규칙(`category_rules`)으로는 닿지 않는 곳이 둘 있습니다.** 규칙은 **앞으로 들어올** 내역만 정하고, 담는 것도 **카테고리뿐**입니다(고정비 여부는 규칙에 없습니다 — 6.4). 그래서 이 둘은 적용 시점에 **행에 직접 씁니다.**

- 함께 옮겨가는 것: `category`, `expenseType`, `isFixedRecurring`, `recurringDay`(고정비는 매달 같은 날 빠지므로 결제일도 따라갑니다. 변동비로 되돌리면 결제일이 떨어집니다).
- **수입은 고정비 판정에서 빼놓습니다.** 고정비/변동비는 지출의 구분이라, 지출의 답을 수입 건에 복사하면 아무도 뜻하지 않은 말이 됩니다. 카테고리만 따라가고 `expenseType`은 그대로 둡니다.
- **수정 중인 건 자신은 제외**합니다 — 폼이 나머지 변경과 함께 직접 저장합니다.
- 이미 같은 분류인 건은 다시 쓰지 않습니다(§14.3 멱등).
- 분류를 **실제로 건드렸을 때만** 범위를 묻습니다.

과거 내역 전체를 카테고리 규칙으로 다시 훑고 싶으면 여전히 `카테고리 관리`의 **`카테고리 일괄 적용`**(6.4)이 있습니다. 그쪽은 규칙 기준이고 **카테고리만** 바꿉니다.

---

## 10. 고정비 판정 (`recurrence.ts`)

고정비/변동비가 이름만으로 모호할 때의 기준입니다.

- **서로 다른 3개월 이상**(`MIN_MONTHS = 3`)에서
- **같은 가맹점/내역명**(공백·특수문자 정규화)이
- **같은 날짜대**(`DAY_TOLERANCE = 4`일, 월말은 `MONTH_END_FROM = 26`일 이후를 같은 대로 취급 — 공휴일·주말 밀림과 말일 차이 보정)

로 반복되면 고정비입니다. 고정비로 판정되면 **거래 일자들에서 매월 결제일을 자동 산정**해 채웁니다. 이 근거를 AI에게도 그대로 전달하고, AI는 이 판단을 우선 신뢰하도록 프롬프트에 지시합니다.

---

## 11. AI 계층

### 11.1 구조

`services/ai/`가 공급자 차이를 흡수합니다. 위 계층은 **평범한 JSON Schema를 넣고 파싱된 JSON을 받습니다.**

| 공급자 | 구현 | 기본 모델 |
| --- | --- | --- |
| Google Gemini | `responseSchema` (타입 대문자) | `gemini-3.8-flash` |
| Claude | strict tool + `tool_choice: auto`, `dangerouslyAllowBrowser: true` | `claude-opus-5` |
| ChatGPT | `response_format: json_schema` strict | `gpt-5.5` |

스키마는 **가장 엄격한 공급자 기준**으로 씁니다: 모든 속성을 `required`에, 모든 객체에 `additionalProperties: false`.

키는 `설정 → AI 등록`에서 공급자별로 등록하고 localStorage에 보관합니다. 키가 없으면 AI 기능만 비활성화되고 나머지는 정상 동작해야 합니다.

**키는 등록한 사용자의 것입니다.** 저장 키가 `smartmoney_ai_providers_v2:<userId>`로 갈리고, 로그인·로그아웃 때 `AuthContext`가 `setAiUser(currentUserId)`로 알려 줍니다(`services/ai/settings.ts`가 DB를 직접 보지 않도록 — 3절 계층 규칙). 사용자를 지우면 `clearAiSettingsFor(id)`가 그 키를 함께 지웁니다. 로그인 전에는 읽지도 쓰지도 않습니다.
→ 사용자별로 갈리기 전에 등록돼 있던 **공용 키는 처음 로그인하는 사람에게 넘어가고 공용 사본은 지워집니다.** 키는 둘로 나눌 수 없고, 공용으로 두면 분리한 뜻이 없어집니다. 다른 사용자는 자기 키를 등록하면 됩니다.

### 11.2 실패 대응

- 재시도 4회. **503(혼잡)은 1.2초 기준, 429(한도)는 15초 기준** 백오프이며 `retryDelay`/`Retry-After`를 우선합니다.
- 한 번이라도 한도에 걸리면 이후 배치 간격을 1.5초 → 13초로 넓힙니다. 빠른 재시도는 남은 할당량을 더 빨리 태웁니다.
- `classifyTransactions`는 **예외를 던지지 않습니다.** `{ results, failed, error }`를 돌려주어 부분 성공을 보존합니다. 배치(40건) 단위로 적용하고, 실패한 건은 선택 상태로 남겨 재시도할 수 있게 합니다.
- 키 오류·네트워크 오류·모델명 오류·할당량 소진은 배치를 반복해도 낫지 않으므로 즉시 중단합니다.
- 사용자에게는 항상 한국어 메시지로 바꿔 보여줍니다. 원문 JSON을 노출하지 마세요.

### 11.3 기능

| 함수 | 용도 |
| --- | --- |
| `analyzeSpending` | 재무 건강도·절약 추천·습관 개선 |
| `classifyTransactions` | 고정비/변동비 + 카테고리 일괄 분류 |
| `parsePaymentMessages` | 결제 문자 붙여넣기 파싱 |
| `askCoach` | 절약 코치 문답 |
| `detectStatementColumns` | 명세서 열 구조 판별 (머리글+표본 5행만 전송) |

AI 자동 분류는 **사용자 규칙 → 자체 판별 → AI** 순으로 적용하고, 끝나면 요약 팝업(고정비 변경·변동비 변경·카테고리 정정·결제일 기입·변경 없음·사용자 규칙 우선 적용·AI 규칙 등록 건수 + 실제 변경 목록)을 띄웁니다. **팝업은 자동으로 닫히지 않습니다.**

---

## 11.4 예산 화면의 세 입력값

세 칸(수입·고정 지출·저축)은 **계산되는 값이 아니라 사용자가 적는 값**입니다. `budget_configs`에 **달마다 따로** 저장되고(PK(user_id, month)), `기본 정보 저장`을 눌러야 저장됩니다.

- **칸 이름은 그 달이 지났는지에 따라 달라집니다**(`actuals.monthPhase` → `budgetWording`). 지난 달의 수입은 예상이 아니라 실적이고, 지난 달의 저축은 목표가 아니라 실제로 떼어 둔 돈입니다.

  | | 지난 달 | 이번 달·다음 달 |
  | --- | --- | --- |
  | 수입 | `8월 총 수입` | `9월 예상 총 수입` |
  | 고정비 | `8월 고정 지출` | `9월 예상 고정 지출` |
  | 저축 | `8월 저축액` | `9월 목표 저축액` |

  → **달 이름을 항상 앞에 붙입니다.** 위쪽 월 이동으로 다른 달을 보면서 이 칸이 어느 달의 값인지 헷갈린 적이 있고(§14.7), 칸 이름이 곧 답입니다.
  → 1번 블록의 제목과 설명도 함께 바뀝니다 — 지난 달에는 "이미 지난 달입니다. 세 칸은 실제로 들어오고 나간 금액"이라고 말합니다.

- **초록 카드(가용 변동비)는 입력칸을 따르고, 2번 블록의 `미배분` 은 저장된 값을 따릅니다.** 괄호 속 내역은 입력칸에서, 합계는 저장된 설정에서 가져오다가 `(수입 7,021,213원 − 고정비 2,765,376원)` 옆에 `0원` 이 찍힌 적이 있습니다 — 한 줄 안에서 두 숫자가 서로 다른 말을 했습니다. 1번은 "저장하면 이렇게 된다", 2번은 "지금 저장된 것"으로 자리를 나눕니다.
- 앱이 계산하는 것은 **`가용 변동비 = 수입 − 고정비 − 저축`**(0 미만이면 0) 하나뿐이고, `AI 스마트 예산 자동 분배`(50/30/20)와 예산 기준의 비율 모드가 모두 이 값을 씁니다. 식은 `budgetPolicy.spareOf` **한 곳에만** 둡니다 — 같은 식을 화면과 컨텍스트에 나눠 두었다가 한쪽만 고쳐질 뻔했습니다.
- **`N월 실제 내역으로 채우기`** 는 그 달 내역에서 수입 합계와 **고정비로 판정된** 건의 합계를 가져옵니다(§10의 판정에 달려 있습니다). **목표 저축액은 채우지 않습니다** — 실적에서 나올 값이 아닙니다.
- **`예상`인데 실적을 채웁니다.** 달이 끝나기 전에 누르면 아직 안 들어온 급여가 빠진 금액이 들어오므로, 이번 달을 보고 있을 때는 지난달을 고르라는 안내를 띄웁니다.
- **값의 출처를 남깁니다** — `income_source`·`fixed_source`·`savings_source`가 `ACTUALS`(실적에서 불러옴) 또는 `USER`(직접 적음)이고, 칸마다 `실적 반영` / `직접 입력` / `수정 중` 배지로 보입니다. 잔액의 `balance_source`(§8)와 같은 취지입니다: 숫자만 남기면 어디서 온 값인지 나중에 알 수 없고, 그 둘은 신뢰도가 다릅니다.
- **배지를 누르면 그 합계를 만든 내역이 열립니다**(`ActualsPickerModal`). 합계만 보여 주면 무엇이 들어갔는지 알 수 없고, 한 번뿐인 상여금이나 이사 비용이 섞이면 그 합계로 세운 예산은 처음부터 틀립니다. 줄 단위로 체크를 풀고 `선택한 금액 적용`을 누르면 그 합만 들어갑니다. **거래 자체는 바뀌지 않습니다.**
- **뺀 항목을 기억합니다**(v16의 `*_excluded`, JSON 배열). 예전에는 합계만 남겨, 8월 수입 칸의 `3,052,140원`(급여 2건)과 목록을 열었을 때의 `8,314,074원`(17건)이 다른 이유를 **아무도 알 수 없었습니다** — 사용자가 "이 금액이 어떤 기준인가요"라고 물은 것이 이것입니다. 숫자만 남기면 출처를 알 수 없다는 것은 잔액(§8)에서 이미 겪은 일이고 답도 같습니다: 판단의 근거를 함께 저장합니다.
  → 다시 열면 **뺀 상태로** 시작하고, 칸 아래에 `8월 실적 17건 중 2건만 반영 · 뺀 15건 5,261,934원`이 보입니다(누르면 목록).
  → **모르는 id 는 조용히 무시합니다**(`sumActuals`). 빼 둔 거래가 지워지거나 다시 가져와 id 가 바뀔 수 있고, 그때 오류를 내면 예산이 통째로 어긋납니다.
  → `N월 실적 채우기`는 전부 채우는 뜻이므로 **제외 목록도 함께 풉니다.**
- **저장된 값은 그때의 스냅샷입니다.** 나중에 명세서를 더 가져오면 실적이 늘어나는데 칸은 그대로라, 차이가 있으면 칸 아래에 `지금 8월 실적은 8,314,074원입니다 · 이 값으로 맞추기`를 띄웁니다. 말해 주지 않으면 틀린 기준으로 예산을 세우게 됩니다.
- **실적의 정의는 `services/actuals.ts` 한 곳에 둡니다.** 같은 정의가 세 곳에서 쓰입니다 — 요약 숫자(`FinanceContext`의 `totalIncome`·`fixedExpenseTotal`·`savingsActualTotal`), 그 숫자를 만든 목록(`ActualsPickerModal`), 그리고 위의 차이 안내. 화면이 각자 걸러 냈을 때 **`저축` 배지를 누르면 고정비 목록이 열렸고**(저축 분기가 없었습니다) 고정비 목록에는 요약에서 빠지는 저축이 섞여 있었습니다.
- **세 칸 모두 실적을 가질 수 있습니다.**
  | 칸 | 실적의 정의 (`actualRows`) |
  | --- | --- |
  | 수입 | 그 달의 `INCOME` 합계 |
  | 고정 지출 | 그 달의 `FIXED` 지출 합계 — **`저축` 제외** |
  | 저축 | 그 달 **계좌에서** `저축` 카테고리로 나간 돈 |
  → **저축은 계좌에서만 셉니다.** 카드로 저축하지 않으므로, 카드 내역에 `저축`이 붙어 있으면 잘못 분류된 것이고 그것까지 세면 숫자가 부풀려집니다.
  → **고정비 합계에서 저축을 뺍니다.** 적금이 매달 같은 날 같은 금액으로 나가 고정비로 판정되더라도, 가용 변동비가 `수입 − 고정비 − 저축`이라 양쪽에 세면 같은 돈이 두 번 깎입니다.
- **입력칸은 입력칸처럼 보여야 합니다.** 이 세 칸이 테두리·배경 없이 굵은 숫자만 놓여 있어 읽기 전용 통계 타일로 보였고, 사용자가 "어디서 입력하나요?"라고 물었습니다. 값을 고치면 저장 버튼이 **`● 저장되지 않음`** 으로 바뀝니다 — 누르지 않고 떠나면 사라지기 때문입니다. `0`은 비워 두어 자리표시자가 보이게 합니다.
- **3열로 나누지 마세요.** 좁은 화면에서 칸이 200px 아래로 줄어 `7,021,2`처럼 **금액이 잘렸습니다.** 세로 한 줄씩 놓고 입력칸에 고정 너비를 줍니다 — 금액은 이 화면에서 가장 먼저 읽혀야 하는 값입니다.

## 11.5 카테고리별 예산 기준 (`services/budgetPolicy.ts`)

열두 카테고리를 매달 손으로 다시 적는 일은 아무도 계속하지 못하고, 그러면 예산은 몇 달 전 값에 멈춘 채 아무 뜻이 없어집니다. **기준을 한 번 정해 두고 어느 달에든 적용합니다** — `budget_policy`는 **달에 매이지 않는 사용자당 한 행**입니다.

- **`지난 내역대로 자동 배분`**(`historyAllocate`)이 기본 수단입니다. 카테고리마다 `한도 = 고정비 월평균 + (변동비 비중 × 가용 변동비)`.
  → 고정비를 더하는 이유: 소진율은 **고정비까지 포함한** 그 달 지출로 계산되므로(`budgetStatusList`), 고정비를 빼놓고 한도를 주면 주거·통신처럼 고정비가 대부분인 카테고리가 달 시작부터 초과로 표시됩니다. 변동비만 비중으로 나누는 이유: 가용 변동비가 이미 고정비를 뺀 값이라 두 번 세지 않기 위함입니다.
  → 어쩌다 한 번 쓴 항목은 **창 안의 달 수로 나눠** 희석합니다. 6개월 중 한 달만 쓴 60만원을 매달 60만원으로 주면 우연이 고정 지출이 됩니다.
  → **예전에는 코드에 박힌 여덟 개 비율**(식비 40%, 쇼핑 18% …)을 곱하면서 화면은 그것을 `AI 스마트 예산 자동 분배`, 토스트는 "50/30/20 규칙"이라고 불렀습니다. **둘 다 사실이 아니었고**, 사용자가 만든 카테고리는 한 푼도 받지 못했습니다. 이름이 하는 일을 말하게 두세요.
- 직접 정하려면 세 가지 방식 중 하나를 고릅니다. `AMOUNT`(금액) · `INCOME_RATIO`(수입의 %) · `SPARE_RATIO`(가용 변동비의 %).
  → **`SPARE_RATIO`의 기준은 `수입 − 고정비 − 저축`입니다.** 저축을 빼지 않으면 비율이 저축 목표를 조용히 먹습니다 — 그 화면에서 사용자가 "건드리지 말라"고 정해 둔 유일한 값입니다.
  → 모드를 바꾸면 **값을 옮기지 않고 비웁니다.** `400,000`이 `400%`가 되면 안 됩니다.
- `[이 기준으로 채우기]`가 그 달 카테고리 예산에 씁니다. **기준에 없는 카테고리는 건드리지 않습니다** — 기준을 적용했다고 손으로 정해 둔 칸을 0으로 만들 이유가 없습니다.
- **합계가 가용 변동비를 넘어도 막지 않습니다.** 여행이 있는 달은 진짜 넘길 수 있습니다. 넘는 금액을 말해 줄 뿐입니다(§17.2 — 근거 없이 대신 결정하지 않습니다).
- **고정비 가이드** (`fixedBaselines`): 카테고리마다 고정비로 매달 얼마가 나가는지 **최근 6개월 평균**과 추이(5% 밖으로 벗어날 때만 ↑↓)를 계산해 **최소선으로 제시**합니다. `[카테고리별 고정비 불러오기]`가 그 평균을 기준값으로 채웁니다(비율 모드에서는 비율로 환산).
  → **진행 중인 달은 평균에서 뺍니다**(`upTo`). 반 달치를 한 달치로 세면 최소선이 실제보다 낮게 제시되고, 그러면 가이드가 아니라 오해가 됩니다.
  → 고정비 판정은 §10이 합니다. AI 자동 분류를 돌리지 않았다면 가이드가 비어 보이는 것이 정상이고, 화면이 그렇게 안내합니다.
  → 한도가 그 평균보다 낮은 카테고리는 **달이 시작되는 순간 이미 초과**이므로 목록과 요약에 표시합니다.
- **`UNSET` 상태**: 예산을 정하지 않은 카테고리는 `SAFE`가 아닙니다. 예전에는 예산 0원에 지출 46만원인 칸이 **초록 막대를 가득 채운 채 "안전 · 100% 소진"** 이라고 적혀 있었습니다. 안전한 것도 초과한 것도 아니라 **아직 정하지 않은 것**입니다.
- 카테고리 한도는 **직접 입력**과 `−`/`+`(1만원) 둘 다 됩니다. 버튼만 있을 때는 45만원을 맞추려면 45번 눌러야 하고 1천원 단위는 아예 불가능했습니다. 입력은 **칸을 떠날 때(또는 Enter) 한 번** 저장합니다 — 누를 때마다 저장하면 `4`만 눌러도 4원이 됩니다.

---

## 12. 화면 구성

하단 탭: `홈` · `소비분석` · `예산·알림` · `AI절약` · `카드·계좌`.

**상단 월 이동 바는 `카드·계좌` 탭에서 감춥니다**(`MobileHeader`의 `MONTHLESS_TABS`). 그 화면은 **지금의** 잔액과 **앞으로** 낼 청구액을 보여 주므로 달을 고를 여지가 없고, `selectedMonth`를 읽는 곳이 한 군데도 없습니다. 눌러도 아무 일이 없는 조작기는 사용자가 자기 잘못을 찾게 만듭니다 — "이게 하는 역할이 무엇인가요?"라는 질문을 받았습니다. **포함이 아니라 제외 목록**인 이유: 나머지 탭은 전부 달에 따라 값이 바뀌므로(컨텍스트의 `transactions`가 이미 그 달로 걸러진 목록), 새 탭에서 바를 잃는 쪽보다 필요 없을 때 빼는 쪽이 안전합니다.

주요 화면과 모달:

| 파일 | 역할 |
| --- | --- |
| `views/ConnectedAssetsView` | 카드·계좌 목록, 등록, DB 관리(내보내기/가져오기/초기화) |
| `transactions/AccountLedgerModal` | 계좌·카드별 내역. 조회 조건·선택·AI 분류·각종 진입점 |
| `modals/CsvImportModal` | 4단계 가져오기 |
| `modals/SmsInboxModal` | 결제 문자 대기함 — 확인·수정·등록 (7.8) |
| `transactions/CategoryRulesModal` | 카테고리 규칙 + 일괄 적용 |
| `transactions/CardUsageModal` | 카드대금에서 연 이용 내역 |
| `modals/AccountEditModal` | 계좌·카드 정보 수정(구분 변경 포함) |
| `modals/BalanceEditModal` | 잔액 + 기준일시 + 출처 |
| `transactions/MonthPickerModal` | 연월 직접 선택(월별 건수 표시) |

### 12.1 계좌 내역 화면의 조건 블록 순서

```
[헤더: 아이콘 · 이름 · 기관·번호 · 연필(정보 수정) · 닫기]
[합계: 계좌=잔액·지출·수입 / 카드=이용 합계]
[버튼 3개: 엑셀·CSV · 직접 추가 · 카테고리 관리]
[AI 자동 분류 + 돋보기(설명 펼침)]
┌ 조회 조건 ──────────────────────────
│ (카드만) 결제월 기준 / 이용일자 기준
│ 월별 / 기간별
│   월별: 2026년 08월  ◀  2026년 09월 ▾  ▶  2026년 10월
│   기간별: [YYYY-MM] 부터 [YYYY-MM] 까지
│ 전체 / 고정비 / 변동비 (/ 수입)
│ (카드만) 전체 / 카드대출 / 일시불 / 할부
│ 카테고리 드롭다운
│ ─────────────
│ ☑ 전체 선택            선택 삭제   조회 N건
│ 선택 N건 합계                    1,234,000원
└──────────────────────────────────
[월별로 묶인 목록]
```

### 12.2 표시 규칙

- **연락처는 `utils/format.ts`의 `formatPhone`으로 끊어 보입니다.** 숫자만 눌러도 `010-1234-5678`이 되고, 입력 중에도 자릿수에 맞춰 자랍니다. `010`은 11자리로 정해져 있어 가운데가 처음부터 4자리이고, 서울(`02`)만 앞자리가 둘입니다. **직접 만들어 쓰지 마세요** — 같은 함수가 화면마다 복사돼 세 벌이 돌아다닌 적이 있습니다.
  → 자릿수 검사와 "바뀌었는가" 판정은 **숫자만** 견줍니다. 하이픈 없이 저장된 옛 값이 화면에서는 하이픈이 붙어 보이므로, 글자로 비교하면 손대지 않아도 바뀐 것으로 잡힙니다.
- **제목 줄 + 버튼은 버튼이 먼저 폭을 잡습니다.** `flex justify-between` 안에서 둘 다 줄어들 수 있게 두면 **버튼 글자가 두 글자씩 갈립니다** — `새로고침`이 두 줄, `7월 실제 내역으로 채우기`가 두 줄이 된 적이 있습니다. 버튼에 `shrink-0 whitespace-nowrap`, 글 묶음에 `min-w-0`을 주세요. 접혀도 읽히는 쪽은 설명 문구이고, 버튼 글자와 금액은 접을 곳이 없는 말입니다.
- **모든 금액에 3자리 콤마**를 씁니다. `utils/format.ts`의 `won`/`withCommas`를 쓰세요. 입력 필드는 `formatAmountInput`/`parseAmountInput` 쌍으로 다룹니다. **`parseInt("1,234,000")`은 1입니다** — 직접 파싱하지 마세요.
- 계좌는 **인디고**, 카드는 **앰버**입니다(`utils/accountTone.ts`). 등록 버튼·요약 카드·목록 아이콘·상세 화면 헤더가 모두 같은 색을 씁니다.
- 데이터가 없으면 **숫자를 지어내지 않습니다.** 분석 전 건강도는 `–`와 `아직 분석 전`으로 표시합니다. 예시 가맹점명을 실제 데이터처럼 보여주지 마세요.

---

## 13. PWA와 배포

```jsonc
// wrangler.jsonc
{ "name": "smart-money", "compatibility_date": "2026-09-10",
  "assets": { "directory": "./dist", "not_found_handling": "single-page-application" } }
```

- Cloudflare: Build command `npm run build`, Deploy command `npx wrangler deploy`. 배포 디렉터리는 `wrangler.jsonc`의 `assets.directory`가 결정합니다.
- GitHub `main`에 푸시하면 Cloudflare가 빌드·배포합니다(1~3분).

### 13.1 무엇이 웹에 올라가는가

**배포되는 것은 `dist/` 뿐입니다.** `vite build`가 만들고, 그 안에 들어가는 것은 번들 결과물과 **`public/`을 그대로 복사한 것**뿐입니다(`publicDir`·`outDir`을 따로 설정하지 않아 기본값입니다). 저장소 루트의 다른 파일은 **서비스되지 않습니다.**

- **`public/`에 둔 것은 그대로 웹에 공개됩니다.** 이것이 이 절의 요점입니다 — 가계부 백업(`.db`·`.smbk`), 명세서 샘플, 메모 따위를 그 안에 두지 마세요. 주소만 알면 누구나 내려받습니다.
- `.gitignore`가 `*.db`·`*.sqlite`·`*.smbk`를 어디서든 걸러 주므로 **GitHub 경유 배포로는** 새지 않습니다. 다만 로컬에서 `npx wrangler deploy`를 직접 하면 git과 무관하게 **작업 폴더의 `public/`이 그대로 실립니다.** 무시 규칙이 이 경로를 막아 주지 않습니다.
- 실제로 저장소 루트 `DB Backup/`에 백업 `.db`가 커밋되어 공개 저장소에 올라간 일이 있었습니다. **웹으로는 서비스되지 않았습니다**(루트는 `dist/`에 복사되지 않으므로) — 한 번 겪어 두었으니, 같은 걱정이 생기면 `find dist -iname "*.db"`로 확인하면 됩니다.
- Cloudflare 빌드 컨테이너는 저장소를 clone하므로 빌드 시점에 그런 파일의 사본을 갖습니다. 일회성 환경이고 산출물에는 포함되지 않습니다.
- `workbox.globPatterns`에 **`wasm`을 반드시 포함**해야 오프라인에서 DB가 열립니다.
- 서비스 워커는 HTTPS 또는 localhost에서만 동작합니다.
- `registerType: 'autoUpdate'`라 **한 번 실행에서 새 버전을 받고 다음 실행에서 화면에 반영**됩니다. 사용자에게는 "두 번 열어보라"고 안내하거나, 새로고침 안내를 띄우세요.
- `ErrorBoundary`가 흰 화면 대신 오류 메시지와 `캐시 비우고 다시 열기`(서비스 워커·캐시 삭제 후 새로고침)를 보여줍니다.

---

## 14. 함정 (반드시 읽을 것)

### 14.1 React 타입이 없습니다

`@types/react`가 설치돼 있지 않습니다. 그래서:

- `useFinance()`의 반환값이 `any`가 되어 **컴포넌트 안의 타입 오류가 `tsc`에 걸리지 않습니다.** `npm run lint` 통과가 컴포넌트의 정확성을 보장하지 않습니다.
- `services/`, `db/`, `utils/`는 제대로 검사됩니다. **중요한 로직은 이 계층에 두고 순수 함수로 만든 뒤 Node 하네스로 검증하세요.**
- 클래스 컴포넌트는 `props`/`setState`를 `declare`로 명시해야 합니다(`ErrorBoundary` 참고).
- `Array.from(anyValue)`는 `unknown[]`이 됩니다. 명시적 타입 주석이 필요합니다.

### 14.2 훅은 조기 반환보다 위에

`if (!isOpen) return null;` **아래에 훅을 추가하면** 열림/닫힘 사이에 훅 개수가 달라져 `Rendered more hooks than during the previous render`로 앱 전체가 죽습니다(흰 화면). 실제로 발생한 사고입니다. 모든 `useState/useMemo/useEffect`는 조기 반환 위에 두세요.

### 14.3 상태 갱신 함수는 순수해야

StrictMode는 업데이터를 두 번 호출합니다. `setState(prev => ...)` 안에서 부수효과를 일으키는 코드는 이 규칙을 알고 작성하세요(이 프로젝트의 `reconcileCardBills`는 멱등하게 작성돼 있습니다). 등록 로직을 업데이터 안에 넣어 두 번 실행되던 버그가 있었습니다.

### 14.4 모달은 `<body>`로 포털

기기 프레임에 `position: relative`가 있어 **스태킹 컨텍스트가 갇힙니다.** 내부 z-index로는 빠져나올 수 없습니다. 모달은 `createPortal(node, document.body)`로 띄우고 아래 층위를 지킵니다.

| z-index | 화면 |
| --- | --- |
| 9998 | 계좌 내역 |
| 9999 | 가져오기, 설정·인증 계열(AI 등록·사용자 관리·앱 설치) |
| 10000 | 거래 추가·수정, 카테고리 관리, 잔액/계좌 수정, 연월 선택, 카드 이용 내역, **간편비밀번호 설정**(사용자 관리 위에 뜹니다) |
| 10001 | AI 분류 결과, 카테고리 일괄 적용 결과 |

Escape 키는 **가장 위 모달만** 닫아야 합니다. 아래 모달의 keydown 핸들러에 위 모달의 열림 상태를 조건으로 넣으세요.

### 14.5 효과 의존성에 인라인 화살표 금지

`onClose`처럼 부모가 매 렌더 새로 만드는 함수를 리셋 효과의 의존성에 넣으면, 부모가 리렌더될 때마다 상태가 초기화됩니다. 결과 팝업이 몇 초 만에 사라지던 버그의 원인입니다. **리셋 효과와 키 리스너 효과를 분리하세요.**

### 14.6 CSS

- `min-height`가 `max-height`를 이깁니다. 프레임 모드에서 화면이 잘리던 원인입니다.
- flex 자식의 `mx-auto`는 교차축 stretch를 무효화합니다. 하단 탭이 가운데로 몰리던 원인입니다.

### 14.7 `useState` 초기값은 한 번만, `reset()`은 남겨 둔 것이 있습니다

**두 번 물린 함정입니다.** 가져오기 모달의 계좌 선택(아래)과 예산 화면의 세 입력칸이 같은 이유로 고장났습니다. 후자는 **월을 옮겨도 값이 바뀌지 않아** "이 월 이동 기능은 무엇을 하는 건가요?"라는 신고로 돌아왔습니다 — 예산은 달마다 따로 저장되고 화면의 나머지는 선택한 달을 따라가는데, `useState(초기값)`으로 잡힌 칸만 첫 렌더에 머물러 있었습니다.

**컨텍스트에서 읽어 입력칸에 채우는 값은 예외 없이** 그 값이 바뀔 계기를 의존성으로 한 효과에서 다시 실어야 합니다.

**그리고 그 계기는 `selectedMonth` 가 아니라 `budgetConfig.month` 입니다.** 여기서 두 번째로 물렸습니다 — `selectedMonth` 가 바뀌는 순간 `budgetConfig` 는 **아직 이전 달의 것**이고, 새 달의 설정은 `refreshDbData` 가 DB 에서 읽어 온 뒤에야 도착합니다. 그 사이에 효과가 돌아 칸에 **이전 달 값**을 싣고, 설정이 도착해도 효과는 다시 돌지 않아 그 값이 남았습니다. 같은 8월을 두 번 봤는데 숫자가 다르고 세 칸이 모두 `수정 중` 으로 보인 이유입니다.

→ **비동기로 실려 오는 값을 의존성으로 쓸 때는 "요청한 것"이 아니라 "도착한 것"을 보세요.** 도착 전에는 `settling` 로 두어 `수정 중` 이라고 말하지 않고 저장 버튼·실적 채우기·실적 목록을 잠급니다 — 그 상태에서 쓰면 `updateBudgetConfig` 가 `prev.month`(아직 이전 달)에 기록하므로 **이 달 숫자가 이전 달 행에 적힙니다.**

→ 그리고 **틈 자체를 줄였습니다.** 달이 바뀌면 `FinanceContext` 가 그 달의 예산 설정만 **동기로** 다시 읽습니다(DB 는 이미 열려 있습니다). 전체 새로고침은 `getDatabase()` 를 기다리는 비동기라 몇 프레임이 걸렸고, 그동안 화면에 `값을 읽는 중` 이 보였습니다. `settling` 은 이제 안전장치로만 남습니다.

모달은 **항상 마운트된 채** `isOpen`으로만 여닫힙니다. 그래서 `useState(props.x || 기본값)`의 초기식은 **앱이 처음 뜰 때 한 번** 계산됩니다 — 그 시점에는 부모가 넘길 값이 아직 없습니다. 나중에 prop이 바뀌어도 상태는 그대로입니다.

가져오기 모달의 계좌 선택이 그랬습니다. 카드 내역 화면에서 열어도 `defaultAccountId`가 무시되고 늘 첫 계좌가 잡혔습니다. `reset()`도 계좌만은 일부러 남겨 두기 때문에(여러 파일을 연달아 넣을 때 매번 고르지 않도록) 닫았다 열어도 마찬가지였습니다.

**prop으로 받은 초기값이 있으면 `isOpen`이 켜지는 순간의 효과로 반영하세요.** 의존성은 `[isOpen, 그 prop]`으로 좁혀, 열려 있는 동안의 사용자 선택과 싸우지 않게 합니다.

### 14.8 개발 서버 포트는 데이터의 일부입니다

**`vite.config.ts`의 `server.port`를 바꾸지 마세요.** `3000`으로 고정돼 있고 `strictPort: true`입니다.

이 앱의 모든 데이터는 IndexedDB에 있고, 브라우저는 **출처(origin)별로** 저장소를 나눕니다. **포트가 출처의 일부**이므로 `localhost:3000`과 `localhost:5173`은 브라우저에게 남남입니다. 포트를 지정하지 않아 Vite 기본값 `5173`으로 뜬 적이 있는데, 앱은 **빈 데이터베이스**를 열었고 사용자가 0명이니 규칙대로 **첫 사용자 등록 화면**을 띄웠습니다. 사용자 2명과 등록한 카드·계좌·내역이 **전부 사라진 것처럼** 보였습니다. 하나도 지워지지 않았는데도요.

`strictPort`는 3000이 이미 쓰이고 있을 때 **조용히 3001로 넘어가지 않고 실패**하게 합니다. 말없이 포트가 바뀌는 것이 곧 "데이터가 사라졌다"이기 때문입니다.

- 배포본(Cloudflare 주소)은 또 다른 출처입니다. 개발 서버의 데이터와 서로 보이지 않는 것이 정상입니다.
- 데이터가 안 보인다는 신고를 받으면 **주소창의 포트부터** 확인하세요. DB 카드의 `저장 크기`가 100KB 안팎에 `0건`이면 갓 만들어진 빈 DB입니다.
- 이때 앱이 "빈 가계부"인 척하지 않도록 막아 둔 것이 4.7입니다. 함께 읽으세요.

### 14.9 소스 편집 도구

Python heredoc 문자열 치환으로 소스를 고치다 **`\b`가 실제 0x08 바이트로 들어가** 조용히 기능이 깨진 적이 있습니다. 백슬래시 이스케이프가 들어가는 편집은 heredoc 대신 파일로 스크립트를 쓰거나 편집 도구를 쓰세요. 치환 후에는 대상이 실제로 바뀌었는지 확인하세요(`grep`).

---

## 15. 검증 방법

### 15.1 명세서 형식 회귀 세트 (고정)

```bash
npm test     # statements + cardlink + spread + format + backup + budget + sms
npm run check  # lint + test
```

`tests/sms.test.ts`는 **결제 문자 파싱과 교차 중복 판정**을 지킵니다(7.8): 카드사별 문자 모양(신한·KB·현대·우리·롯데·카카오뱅크·국민은행), 누적·잔액을 거래액으로 읽지 않을 것, 연도 없는 날짜와 해 넘김, 취소·환불의 방향, 거래가 아닌 문자를 들이지 않을 것, 여러 건 쪼개기와 줄바꿈 문자 보존, 그리고 **이름이 달라도 같은 거래를 찾아낼 것**(방향·계좌·날짜·금액은 같아야 함).
**새 카드사 문자가 안 읽히면 그 원문을 이 파일에 케이스로 추가하세요.**

`tests/budget.test.ts`는 **예산 기준·내역 기반 배분·고정비 가이드**를 지킵니다(11.5): 가용 변동비 계산, 세 방식의 배분, 초과를 막지 않고 알릴 것, 규칙 없는 카테고리를 0으로 만들지 않을 것, 카테고리별 월평균과 추이, **진행 중인 달을 평균에서 뺄 것**, 가이드↔기준 환산, 고정비보다 낮은 한도 집어내기, 내역 기반 배분(고정비+변동 비중·어쩌다 한 번 희석·사용자 카테고리 포함), **`저축`과 `카드대금`이 예산에서 빠질 것**.
같은 파일이 **실적의 정의**(§11.4)도 지킵니다: 세 칸이 각각 무엇을 세는지(수입 전부 / 고정비에서 저축 제외 / 저축은 계좌에서만), 다른 달을 섞지 않을 것, 계좌를 모르면 저축을 지어내지 않을 것, 뺀 항목이 합계에서 빠지고 전체 금액은 그대로 알 수 있을 것, **모르는 id 를 무시할 것**, 그리고 **지난 달에는 `예상`·`목표`라는 말을 쓰지 않을 것**.

`tests/backup.test.ts`는 **백업 암호화**를 지킵니다(4.5): 왕복, **형식 제한이 없을 것**(1자리·한글·이모지·500자·앞뒤 공백 보존), 틀린 암호와 **한 바이트 변조**를 거절할 것, 평문 `.db`를 "암호화 아님"으로 구분할 것, 안내 문구가 막지 않을 것.

`tests/format.test.ts`는 **표시 형식**을 지킵니다: 연락처 끊어 보이기(입력 중·서울·11자리 초과·멱등), 금액 콤마 왕복(`parseInt("1,234,000")`은 1입니다 — 12.2).

`tests/spread.test.ts`는 **같은 내역명 전체 적용**을 지킵니다(9.6): 가맹점 이름 비교(공백·대소문자), `이 건만`/`같은 내역명 모두`, 이미 적힌 설명은 기본적으로 건너뛸 것, `덮어쓰기`를 골랐을 때만 바꿀 것, 카테고리·고정비·결제일이 함께 옮겨갈 것, **수입 건에는 고정비를 퍼뜨리지 않을 것**, 다른 계좌·다른 가맹점은 제외할 것, 같은 값이면 쓰지 않을 것(§14.3).

`tests/cardlink.test.ts`는 **카드 청구액 ↔ 은행 출금 연결**을 지킵니다: 카드·은행 어느 쪽이 들어와도 연결될 것, 금액이 정확히 같을 때만 연결할 것, 한 출금은 한 명세서에만, 다른 카드가 가져간 출금은 빼앗지 않을 것, 결제 계좌가 없는 카드는 카드사가 유일할 때만, 해지는 카드 쪽이 바뀌었을 때만, 그리고 같은 입력에 두 번 돌려도 결과가 같을 것(§14.3).

`tests/statements.test.ts`에 **지원하는 모든 명세서 형식**이 기대값과 함께 들어 있습니다.

| 케이스 | 무엇을 지키는가 |
| --- | --- |
| 통장 거래내역 | 제목 줄 건너뛰기, 출금/입금 분리, 잔액 제외, 시각 |
| KB국민카드 | 두 줄 머리글 병합, 회차가 금액이 되지 않을 것, 소계·합계·무이자 환급·할인 줄 제외, **원금 + 수수료가 명세서 합계와 일치할 것** |
| 신한카드 | 파일 중간의 표 찾기, 납부금액 원금, 섹션 경계에서 끊기, 결제월을 준 경우의 연회비 |
| 삼성카드 | 가맹점 칸에 적힌 합계 제외(긴 이름 포함), 이용금액이 아닌 `원금`, 8자리 이용일, 합계 일치 |
| 삼성카드 2시트 | 할부·리볼빙 시트를 한 번에 읽을 것, 시트를 고르면 그 시트만 읽을 것 |
| 우리카드 | 이름 가운데에 든 `소계(…)`·`청구합계-…` 제외, `매출구분`이 메모, 음수 차감이 지출이 아닐 것 |

`tests/cardlink.test.ts`는 여기에 더해 **카드 자기 내역이 명세서와 연결되지 않을 것**(9.1)과 **차감이 그 달 청구액에서 빠질 것**을 지킵니다.
| 롯데카드 | `입금하실 금액`이 수입이 아닐 것, 수수료만 있는 줄, 합계 일치 |
| 결제금액이 빈 명세서 | 이름이 아니라 데이터로 금액 열 다시 찾기 |
| 날짜 12종 | 4자리·2자리 연도, 연도 없음, 월>12 거부 |
| 할부 회차 | 중복 키에 회차가 반영될 것 |
| 롯데 .xls(HTML) | colspan·rowspan 머리글 병합, 텍스트 경로와 같은 결과 |
| 결제월 추정 | 파일 이름 22종(신한 `YYYY년M월`·롯데 `YYMM`·KB `YYYYMMDD`·생성일시), 이름이 없을 때 이용월+1, 연말 넘김, 할부 제외 |
| 기억된 형식 | 낡은 지정이 물러서고, 맞는 지정·사용자 선택은 유지될 것, 나중에 생긴 열이 옛 기억에 채워질 것 |
| 할부 회차 열 | 회차가 메모에 실리고, 다음 달 같은 할부가 중복이 아닐 것 |
| `.xls` 3형태 | 텍스트(한글 유지)·HTML(표마다 시트)·진짜 워크북(시트 선택) |

**새 카드사 형식을 지원할 때마다 이 파일에 케이스를 추가하세요.** 건수만 세지 말고 날짜·가맹점·금액·구분까지 확인해야 합니다 — 엉뚱한 열을 읽어도 건수는 맞을 수 있습니다.

**한 형식을 고치면 다른 형식이 깨집니다.** 실제로 그래 왔습니다: 열 이름을 넓히면 다른 명세서의 포인트·회차 열이 걸려들고, 표 찾기를 바꾸면 요약 블록이 선택됩니다. 커밋 전에 `npm run check`를 돌리세요.

### 15.2 일회용 하네스

회귀 세트에 넣기 애매한 일회성 확인은 임시 파일로 하고 지웁니다.

```bash
npx tsx ./__something.test.ts
```

이렇게 검증해 온 것들: 마이그레이션(v0부터 전체 실행, 중간 버전 업그레이드, 재실행 멱등성, 데이터 보존, 컬럼·테이블 자가복구), 카테고리 판정 우선순위, 카드사 매칭과 결제월 대조, 청구 예정액 3기준, 잔액 조정 산식, PIN 해시, 반복 결제 판정.

sql.js를 노드에서 쓸 때는 `initSqlJs({ locateFile: () => "./node_modules/sql.js/dist/sql-wasm.wasm" })`로 초기화합니다.

## 16. 커밋

- 한 커밋에 한 가지 변화. 제목은 **무엇을 하는지**(명령형), 본문은 **왜 그렇게 했는지**와 어떤 문제를 고쳤는지.
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- `finance.db`는 `.gitignore`에 있습니다. 저장소에도 빌드 결과물에도 들어가지 않습니다.

---

## 17. 앞으로 손댈 때 지킬 것

1. **데이터를 지어내지 않습니다.** 모르면 `–`로 두고 왜 모르는지 적습니다.
2. **추측으로 연결하지 않습니다.** 카드·명세서·중복 판정 모두, 근거가 없으면 비워 두고 사용자가 정하게 합니다.
3. **기존 데이터를 조용히 버리지 않습니다.** 화면에 없는 필드라도 저장 시 유지하세요(결제월을 날려먹은 적이 있습니다).
4. **구조를 바꾸면 마이그레이션과 `EXPECTED_*` 목록을 함께 고칩니다.**
5. **로직은 `services/`의 순수 함수로**, 화면은 그것을 부르기만 하도록. 타입 검사가 닿는 곳이 거기뿐입니다.
6. 새 판정 규칙을 넣을 때는 **가져오기와 마이그레이션이 같은 표를 쓰게** 하세요. 과거 데이터와 새 데이터가 달리 분류되면 합계가 맞지 않습니다.
