# 스마트 머니 — 개발 지침서 (v1.0)

이 문서 묶음은 **이것만 가지고 같은 프로그램을 다시 만들 수 있도록** 구조·규칙·판단 근거를 기록한 것입니다. 화면 사진이나 코드 전문 대신 **왜 그렇게 되어 있는가**를 남깁니다 — 그 까닭이 대부분 실제로 겪은 사고에서 왔고, 까닭을 모르면 같은 사고를 다시 겪기 때문입니다.

> **이 문서를 코드와 함께 고칩니다.**
> 프로그램을 바꾸면 영향받는 절을 같은 커밋에서 갱신합니다. 문서가 코드와 어긋나는 순간
> 이 문서는 목적을 잃고, 다음 작업이 잘못된 전제 위에서 시작됩니다.
>
> **명세서 형식을 건드렸다면 `tests/statements.test.ts`에 케이스를 추가하고
> `npm run check`를 돌린 뒤 커밋합니다.** 한 카드사를 지원하려다 다른 카드사를
> 깨뜨리는 일이 반복해서 있었습니다.

---

## 0. 이 묶음을 읽는 법

분량이 한 파일에 담기 어려워 절 단위로 나누었습니다. **절 번호는 나누기 전과 같습니다** — 소스 주석 284곳이 `§7.6`·`§14.4` 같은 번호로 이 문서를 가리키고 있어, 번호가 바뀌면 그 참조가 전부 거짓이 됩니다.

| 절 | 파일 | 내용 |
| --- | --- | --- |
| 0~3 · 14 · 16 · 17 | **CLAUDE.md** (이 파일) | 앱의 성격 · 스택 · 구조 · **함정** · 커밋 · **원칙** |
| 4 · 5 · 8 | [docs/DATA.md](docs/DATA.md) | 데이터베이스 · 마이그레이션 · 저장과 실패 · 백업 · 되돌리기 · 인증 · 잔액 |
| 6 · 10 · 11 | [docs/DOMAIN.md](docs/DOMAIN.md) | 카테고리 · 고정비 판정 · AI 계층 · 예산 · 분석 · 알림 |
| 7 · 9 | [docs/IMPORT.md](docs/IMPORT.md) | 명세서 가져오기 · 결제 문자 · 여러 파일 · 카드-계좌 연결 |
| 12 · 13 | [docs/UI.md](docs/UI.md) | 화면 구성 · 표시 규칙 · PWA와 배포 |
| 15 | [docs/TESTING.md](docs/TESTING.md) | 회귀 세트 16종 · 일회용 하네스 |
| — | [docs/SETUP.md](docs/SETUP.md) | **빈 폴더에서 이 앱까지** — 설정 파일 전문과 만드는 순서 |

**처음 읽는다면**: 1절(성격) → 3절(구조) → [SETUP](docs/SETUP.md)(세우기) → [DATA](docs/DATA.md)(데이터 모델) → 나머지.
**코드를 고치러 왔다면**: **17절(원칙)과 14절(함정)을 먼저** 읽으세요. 그 둘이 이 저장소에서 가장 자주 참조되는 절입니다.

---

## 1. 이 앱이 무엇인가

한국 개인용 가계부 PWA입니다. 은행 통장과 카드사 명세서를 **엑셀·CSV로 가져와** 분류하고, 고정비/변동비를 가려내고, 예산과 절약을 관리합니다.

세 가지가 이 앱의 성격을 결정합니다.

1. **서버가 없습니다.** 모든 데이터는 기기 안 SQLite(sql.js, WebAssembly)에 있고 IndexedDB에 저장됩니다. 백엔드도, 계정 서버도 없습니다.
2. **AI는 사용자의 키로 브라우저에서 직접 호출합니다.** 키는 기기에만 있고, AI 없이도 앱의 모든 기본 기능이 동작해야 합니다.
3. **금융기관 자동 연동은 없습니다.** 오픈뱅킹은 사업자 등록이, 스크래핑은 마이데이터 라이선스가 필요합니다. 개인 사용자는 **명세서 파일 가져오기**가 유일한 현실적 경로입니다. 이 전제를 뒤집지 마세요.

이 세 가지에서 나머지가 따라옵니다 — 계정 서버가 없으니 PIN이 유일한 잠금이고(§5), 서버가 없으니 웹푸시가 불가능하며(§11.8), 자동 연동이 없으니 명세서 해석의 정밀도가 이 앱의 값 전부입니다(§7).

### 1.1 무엇을 하는 앱인가 (v1.0 기준 기능 전체)

| 묶음 | 기능 |
| --- | --- |
| 넣기 | 엑셀·CSV 명세서 가져오기(카드 6개사·은행 통장, 여러 파일 동시) · 결제 문자 등록 · 직접 입력 |
| 가려내기 | **방향별 카테고리**(수입 4 · 지출 17) · 사용자 규칙 · **고정/변동 판정(수입에도 — §6.6)** · AI 일괄 분류 · 같은 내역명 전체 적용 |
| 잇기 | 카드 명세서 ↔ 은행 출금 자동 연결 · 청구 예정액 산출 · 잔액 조정 |
| 보기 | 홈 결산 · 소비분석(월·연·기간 추이) · 카테고리 드릴다운 · 내역 찾기와 CSV · 정기 결제 모아 보기 |
| 관리 | 월별 예산과 기준 정책 · 카테고리 한도와 소진율 · 기기 알림 · AI 절약 분석과 코치 |
| 지키기 | 백업·복원(암호화 선택) · 되돌리기 7일 · 데이터 점검 · 다중 사용자 분리 |

### 1.2 v1.0 에 **일부러** 없는 것

다시 만드는 사람이 "빠뜨렸나" 하고 찾지 않도록 적어 둡니다. 아래는 미완이 아니라 **선택**입니다.

| 없는 것 | 까닭 |
| --- | --- |
| 금융기관 자동 연동 | 사업자 등록·마이데이터 라이선스가 필요합니다(§1). 이 전제를 뒤집으면 다른 앱이 됩니다 |
| 계정·서버·다기기 동기화 | 서버를 두는 순간 "데이터가 기기를 떠나지 않는다"가 거짓이 됩니다 |
| 앱을 닫은 뒤의 푸시 알림 | 웹푸시는 발송 서버와 VAPID 키를 요구합니다(§11.8). 화면이 그 한계를 정직하게 적습니다 |
| 기기 안 DB 암호화 | PIN 에서 키를 유도하면 PIN 을 잊었을 때 복구 경로가 없습니다. 백업 파일만 암호화합니다(§4.5) |
| 현금·투자·부동산 자산 | 계좌와 카드만 다룹니다. 현금 지갑은 v1.1 후보입니다 |
| 광고·금융상품 추천 | 분석의 동기가 오염됩니다 |
| 영수증 첨부 · 태그 · 다중 통화 | 아직 필요를 겪지 않았습니다 |

---

## 2. 기술 스택과 명령

| 항목 | 내용 |
| --- | --- |
| 프레임워크 | React 19 + TypeScript 5 + Vite 6 |
| 스타일 | Tailwind CSS 4 (`@tailwindcss/vite`) |
| 아이콘 | `lucide-react` |
| 차트 | `recharts` (소비분석 탭에서만 — 지연 로드) |
| DB | `sql.js` 1.14 (WASM SQLite) + IndexedDB |
| 엑셀 | `xlsx` **0.20.3, npm 아닌 SheetJS CDN tarball** |
| QR | `qrcode` (설치 안내용, 이 기기에서 생성) |
| PWA | `vite-plugin-pwa` (`registerType: 'autoUpdate'`) |
| AI SDK | `@google/genai`, `@anthropic-ai/sdk`, OpenAI는 `fetch` |
| 검사 | `typescript-eslint` + `eslint-plugin-react-hooks` v6 |
| 배포 | Cloudflare Workers(정적 자산) + GitHub `main` 푸시 |

설정 파일 전문과 **빈 폴더에서 세우는 순서**는 [docs/SETUP.md](docs/SETUP.md)에 있습니다.

```bash
npm run dev      # 개발 서버 — 반드시 http://localhost:3000 (14.8)
npm run lint     # tsc --noEmit + eslint (훅 규칙·죽은 코드)
npm test         # 회귀 세트 16종 · 1,203개 확인 (15절)
npm run check    # lint + test — 커밋 전에 이것을 돌립니다
npm run build    # vite build → dist/
```

빌드 시각이 번들에 박혀(`__BUILD_TIME__`, `vite.config.ts`의 `define`) **카드·계좌 화면의 DB 카드에 표시**됩니다. PWA는 한 번 실행에서 새 버전을 받고 다음 실행에서 보여주기 때문에, "고친 게 반영된 버전인가"를 기기에서 확인할 방법이 필요합니다. `package.json`의 `version`은 저장소의 표시일 뿐 화면에 나오지 않습니다.

**의존성 주의**
- `xlsx`는 반드시 `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`로 설치합니다. npm 레지스트리의 `xlsx@0.18.5`에는 미패치 취약점 2건이 있습니다.
- **`@types/react` 는 설치돼 있습니다.** 오랫동안 없었고, 그동안 컴포넌트 계층(전체의 60%)이 검사 밖에 있었습니다 — §14.1을 읽으세요.
- `qrcode` 는 설치 안내의 QR 을 **이 기기에서** 만듭니다. 예전에는 외부 서비스(`api.qrserver.com`)에 주소를 넘겨 이미지를 받아 왔고, 오프라인에서 깨졌습니다.
- **미사용 의존성을 남기지 마세요.** `motion` 이 한 곳에서도 쓰이지 않은 채 들어 있었습니다.
- 락파일은 `package-lock.json` 하나만 둡니다. `bun.lock` 이 함께 있던 때, 호스팅 빌드가 어느 쪽을 잡느냐에 따라 이미 지운 서버 의존성으로 설치가 흘러간 적이 있습니다.

---

## 3. 디렉터리 구조

**계층 규칙**: 화면 → Context → repository → database. 화면이 `repository`나 `database`를 직접 부르지 않습니다(내보내기/가져오기 등 일부 관리 기능 예외). `services/`는 순수 로직이며 React를 모릅니다.

이 규칙이 장식이 아닌 이유는 §17.5에 있습니다 — 화면 안에 있던 계산을 서비스로 옮길 때마다 **두 화면이 서로 다른 말을 하고 있던 것**이 드러났습니다.

### 3.1 뿌리

```
├── CLAUDE.md · docs/          이 문서 묶음
├── README.md                  앱 소개와 빠른 시작
├── package.json               스크립트와 의존성 (version: 1.0.0)
├── vite.config.ts             Vite · Tailwind · PWA · 포트 3000 고정 · __BUILD_TIME__
├── eslint.config.js           좁은 규칙만 (§14.9)
├── tsconfig*.json             strict
├── wrangler.jsonc             Cloudflare 정적 자산 배포
├── index.html                 진입 HTML
├── public/                    아이콘·매니페스트 자산 — 여기 둔 것은 그대로 웹에 공개됩니다 (§13.1)
├── scripts/                   PWA 아이콘 생성
├── src/                       아래 3.2
├── tests/                     회귀 세트 16종 (§15)
└── .github/workflows/check.yml  main 푸시·PR 마다 check + build
```

### 3.2 `src/`

```
src/
├── main.tsx                  ErrorBoundary로 감싼 진입점
├── App.tsx                   인증 게이트 · 탭 전환 · 프레임/와이드 모드 · 공용 모달 호스트
├── types/finance.ts          모든 도메인 타입 (§4.4와 짝)
├── constants/categories.ts   카테고리 목록 · 키워드 표 · 카드사 목록 (단일 출처)
├── db/
│   ├── schema.ts             SCHEMA_VERSION · MIGRATIONS · migrate · 자가복구 · 샘플 데이터
│   ├── database.ts           sql.js 초기화 · IndexedDB 저장 · 원자적 배치 · 내보내기/가져오기
│   └── repository.ts         모든 SQL. 화면은 이 파일을 직접 부르지 않습니다
├── context/
│   ├── AuthContext.tsx       사용자 등록·로그인·로그아웃 · AI 키 소유자 전환
│   └── FinanceContext.tsx    앱 상태의 중심. 모든 쓰기가 여기를 지남
├── services/                 순수 로직 — 아래 3.3
├── components/               화면 — 아래 3.4
└── utils/
    ├── format.ts             금액·연락처·기준일시 표시 (§12.2)
    └── accountTone.ts        계좌 인디고 / 카드 앰버
```

### 3.3 `services/` — 판정이 사는 곳 (26개)

회귀 세트가 닿는 유일한 계층입니다. **새 판정 규칙은 반드시 여기에**(§17.5).

| 파일 | 하는 일 | 절 |
| --- | --- | --- |
| `csvImport.ts` | 파일 해독 · 표 인식 · 열 매칭 · 값 해석 · 초안 · 중복 키 | §7.2~7.6 |
| `importQueue.ts` | 여러 파일 대기줄 · 새 것/중복 가르기 | §7.10 |
| `cardLabels.ts` | 명세서가 부르는 카드 이름 기억 (자동 식별) | §7.10 |
| `statementFormats.ts` | 확인된 열 지정 기억 (localStorage) | §7.4 |
| `smsParse.ts` | 결제 문자 규칙 파싱 · 유사 건 찾기 | §7.8 |
| `cardLink.ts` | 카드↔출금 연결 · 결제월 대조 · 청구예정액 · 표기 | §9 |
| `categoryRules.ts` | 패턴 규칙 · 카드대금/금융 자체 판별 · 카드사 목록 | §6.2~6.4 |
| `spread.ts` | 한 건의 수정을 같은 내역명 전체로 | §9.6 |
| `recurrence.ts` | 고정비 반복 판정 · 정기 결제 목록 | §10 · §12.10 |
| `actuals.ts` | 실적의 정의 · 옮긴 돈 제외 · 달의 단계 | §6.5 · §11.4 |
| `budgetPolicy.ts` | 가용 변동비 · 배분 · 고정비 가이드 | §11.5 |
| `budgetStatus.ts` | 카테고리 소진율 판정 · 예산 알림 | §11.5 · §11.8 |
| `history.ts` | 달·해 집계 · 카테고리 비중 | §12 |
| `trend.ts` | 기간 값 · 추이 · 연월 한 칸 다루기 | §12.4 |
| `ledger.ts` | 계좌·카드 내역 화면의 산식 (카드 합계 포함) | §9.5 · §12.1 |
| `query.ts` | 내역 찾기 · 유사 항목 · CSV 내보내기 | §12.9 |
| `analysisFreshness.ts` | AI 분석 스냅샷의 신선도 | §11.6 |
| `notify.ts` | 기기 알림 | §11.8 |
| `onboarding.ts` | 첫 사용 안내 네 걸음 | §12.11 |
| `undo.ts` | 되돌리기 보관 기간과 판정 | §4.9 |
| `validate.ts` | **쓰기 전 검증 — 모든 경로가 지남** | §17.7 |
| `integrity.ts` | 이미 들어온 것 점검 · 좀비 가려내기 | §17.7 |
| `balance.ts` | 잔액 조정 산식 | §8 |
| `backupCrypto.ts` | 백업 암호화 (AES-256-GCM) | §4.5 |
| `pinCrypto.ts` | PBKDF2 PIN 해시 | §5 |
| `aiClient.ts` + `ai/` | 프롬프트 · JSON 스키마 · 공급자 어댑터 · 재시도 | §11.1~11.3 |

### 3.4 `components/` — 화면 (42개)

```
components/
├── ErrorBoundary.tsx          흰 화면 대신 오류와 "캐시 비우고 다시 열기"
├── layout/
│   ├── MobileHeader.tsx       기준월 이동 바 · 설정 메뉴 (§12)
│   └── BottomNavigation.tsx   하단 5탭
├── auth/
│   ├── PinAuthScreen.tsx      사용자 선택 → PIN
│   ├── PinPad.tsx             6자리 키패드
│   ├── PinSetupModal.tsx      PIN 등록·변경 (z-10000)
│   ├── UserRegistrationSteps.tsx  첫 실행 3단계 등록
│   └── UserSecurityModal.tsx  내 정보
├── views/                     탭 화면과 전체 화면
│   ├── HomeView.tsx           결산 · 바로가기 · 자산 요약 (§12.7)
│   ├── AnalyticsDashboardView.tsx  소비분석 3모드 (지연 로드)
│   ├── PeriodTrendPanel.tsx   기간 추이 (§12.4)
│   ├── BudgetManagementView.tsx    예산 네 항목 · 카테고리 한도 (§11.4)
│   ├── AISavingsCoachView.tsx AI 분석 · 절약 추천 · 코치 문답
│   ├── ConnectedAssetsView.tsx     카드·계좌 · DB 관리 (지연 로드)
│   ├── LedgerView.tsx         전체 내역 (홈의 링크로 진입)
│   └── FixedVsVariableView.tsx     고정비/변동비 분리 (홈의 링크로 진입)
├── dashboard/SummaryCard.tsx  홈 결산 카드 — 네 금액이 눌립니다 (§12.7)
├── transactions/
│   ├── AccountLedgerModal.tsx 계좌·카드별 내역 (z-9998, §12.1)
│   ├── AddTransactionModal.tsx     거래 추가·수정 (z-10000)
│   ├── TransactionItem.tsx    한 줄 · 아이콘 · 색
│   ├── CategorySelect.tsx     카테고리 고르기 (+직접 입력)
│   ├── CategoryRulesModal.tsx 규칙 목록 · 일괄 적용
│   ├── CategoryApplyResultModal.tsx  일괄 적용 결과 (z-10001)
│   ├── ClassifyResultModal.tsx      AI 분류 결과 (z-10001)
│   ├── CardUsageModal.tsx     카드대금에서 연 이용 내역
│   └── MonthPickerModal.tsx   연월 직접 선택 — 모든 자리가 이 창 (§12.6)
├── modals/
│   ├── CsvImportModal.tsx     4단계 가져오기 (지연 로드, §7.1)
│   ├── SmsInboxModal.tsx      결제 문자 대기함 (§7.8)
│   ├── SearchModal.tsx        내역 찾기 · CSV (§12.9)
│   ├── RecurringModal.tsx     정기 결제 모아 보기 (§12.10)
│   ├── CategorySpendingModal.tsx    카테고리 합계의 속 (§11.7)
│   ├── ActualsPickerModal.tsx 실적을 만든 내역 (§11.4)
│   ├── BudgetPolicyModal.tsx  예산 기준 정책 (§11.5)
│   ├── AccountEditModal.tsx   계좌·카드 수정
│   ├── BalanceEditModal.tsx   잔액 + 기준일시 + 출처 (§8)
│   ├── BackupPassphraseModal.tsx    백업 암호 (§4.5)
│   └── ConfirmModal.tsx       되돌리기 어려운 일의 확인 (z-10002, §12.8)
├── settings/
│   ├── AIKeyModal.tsx         공급자별 키 등록 (§11.1)
│   ├── UserManageModal.tsx    사용자 추가·수정·삭제 (§5)
│   ├── MiscSettingsModal.tsx  기타 설정 — 되돌리기 · 데이터 점검
│   └── DataCheckModal.tsx     데이터 점검 (§17.7)
└── pwa/PWAInstallButton.tsx   설치 안내 · 오프라인 배너 (§13.2)
```

**모달 층위(z-index)는 §14.4의 표가 단일 출처입니다.** 새 창을 만들 때 그 표를 먼저 보세요.

---

## 14. 함정 (반드시 읽을 것)

### 14.1 React 타입은 이제 있습니다 (오래 없었습니다)

**`@types/react` 가 설치돼 있습니다.** 그 전까지 `useFinance()` 의 반환값이 `any` 가 되어 **컴포넌트 계층 전체(코드의 60%)가 `tsc` 밖에** 있었습니다. 이 대화에서 발견한 컴포넌트 버그가 전부 그 종류였습니다 — 목록을 잘못 본 세 곳, 코드에 박힌 `9월`, 데이터에 없는 기본값.

**설치의 영향은 오류 6곳이었고, 여섯이 모두 실제 버그였습니다.** 가장 큰 것:

- `ConnectedAccount["type"]` 이 `"BANK" | "CREDIT_CARD" | "CHECK_CARD"` 로 선언돼 있었는데 **저장되는 값은 처음부터 `BANK`·`CARD`** 였습니다(§4.4가 그 사실을 적어 두고 있었습니다). 그래서 `repository` 의 `type IN ('CREDIT_CARD','CHECK_CARD')` 가 **한 건도 맞히지 못한 채** 조용히 아무 일도 하지 않았고, 그것이 **카드의 `balance_or_billed` 가 갱신되지 않던 원인**입니다. 타입을 실제 값으로 바로잡았습니다.
- `updateAccountDetails` 의 선언에 `paymentAccountId` 가 빠져 있었습니다(repository 는 처음부터 받고 있었습니다).
- `ErrorBoundary` 의 `declare props/setState` 는 더 필요하지 않아 지웠습니다.

**여전히 남는 것:**

- 컴포넌트 안의 `any` 는 **42곳**이었습니다. `@types/react` 가 없던 동안 쌓인 빚이고, 지금은 **2곳**만 남았습니다(아래).
- `Array.from(anyValue)`는 `unknown[]`이 됩니다. 명시적 타입 주석이 필요합니다.

- **남은 `any` 는 DB 계층 22곳과 화면 2곳뿐입니다.** SQL 행의 모양은 그쪽에서 정당한 편이고, 화면의 둘은 **`recharts` 의 클릭 payload** 입니다 — 버전마다 모양이 달라(`name` 이 바로 오기도, `payload` 안에 들어오기도) 라이브러리가 타입으로 내주지 않습니다. 그 까닭을 코드에 적어 두었습니다. 나머지는 정리했습니다: `editingTx` 여섯 곳이 `Transaction | null` 이 되었고, 컨텍스트가 내주는 값을 `any` 로 받던 곳들도 그대로 쓰면 되는 것이었습니다.
- **`alert` 도 없앴습니다**(§12.8 이 `window.confirm` 열 곳을 없앤 것과 같은 까닭). 거래 등록의 `금액과 가맹점을 입력해주세요` 는 **검증 결과를 칸 아래에 적는 것**으로 바뀌었고(§17.7 — 날짜까지 봅니다), 계좌 등록도 같습니다. 백업 복원 뒤의 안내는 앱의 확인 창이 맡고, **그 창을 닫을 때 로그아웃**합니다 — 복원된 파일에 지금 세션의 id 가 없을 수 있어 그대로 두면 모든 조회가 0건이 되기 때문입니다(§4.6).
- **화면은 이제 `repository` 를 한 곳도 직접 부르지 않습니다.** `accountFootprint` 가 마지막이었고 컨텍스트를 지나게 했습니다(§3 계층 규칙). 내보내기·가져오기는 예외로 남습니다.
- **`tsc` 통과가 여전히 정확성을 뜻하지는 않습니다.** 목록을 잘못 고르는 종류의 버그는 타입이 잡지 못합니다(세 번 겪었습니다 — §11.7). 중요한 로직은 계속 `services/` 의 순수 함수로 두고 회귀 세트로 지키세요.

### 14.2 훅은 조기 반환보다 위에

`if (!isOpen) return null;` **아래에 훅을 추가하면** 열림/닫힘 사이에 훅 개수가 달라져 `Rendered more hooks than during the previous render`로 앱 전체가 죽습니다(흰 화면). 실제로 발생한 사고입니다. 모든 `useState/useMemo/useEffect`는 조기 반환 위에 두세요.

### 14.3 상태 갱신 함수는 순수해야

StrictMode는 업데이터를 두 번 호출합니다. `setState(prev => ...)` 안에서 부수효과를 일으키는 코드는 이 규칙을 알고 작성하세요(이 프로젝트의 `reconcileCardBills`는 멱등하게 작성돼 있습니다). 등록 로직을 업데이터 안에 넣어 두 번 실행되던 버그가 있었습니다.

### 14.4 모달은 `<body>`로 포털

기기 프레임에 `position: relative`가 있어 **스태킹 컨텍스트가 갇힙니다.** 내부 z-index로는 빠져나올 수 없습니다. 모달은 `createPortal(node, document.body)`로 띄우고 아래 층위를 지킵니다.

| z-index | 화면 |
| --- | --- |
| 9998 | 계좌 내역 |
| 9999 | 가져오기, 설정·인증 계열(AI 등록·사용자 관리·앱 설치) |
| 10000 | 거래 추가·수정, 카테고리 관리, 잔액/계좌 수정, 연월 선택, 카드 이용 내역, **간편비밀번호 설정**(사용자 관리 위에 뜹니다) |
| 10001 | AI 분류 결과, 카테고리 일괄 적용 결과 |

Escape 키는 **가장 위 모달만** 닫아야 합니다. 아래 모달의 keydown 핸들러에 위 모달의 열림 상태를 조건으로 넣으세요.

### 14.5 효과 의존성에 인라인 화살표 금지

`onClose`처럼 부모가 매 렌더 새로 만드는 함수를 리셋 효과의 의존성에 넣으면, 부모가 리렌더될 때마다 상태가 초기화됩니다. 결과 팝업이 몇 초 만에 사라지던 버그의 원인입니다. **리셋 효과와 키 리스너 효과를 분리하세요.**

### 14.6 CSS

- `min-height`가 `max-height`를 이깁니다. 프레임 모드에서 화면이 잘리던 원인입니다.
- flex 자식의 `mx-auto`는 교차축 stretch를 무효화합니다. 하단 탭이 가운데로 몰리던 원인입니다.

### 14.7 `useState` 초기값은 한 번만, `reset()`은 남겨 둔 것이 있습니다

**두 번 물린 함정입니다.** 가져오기 모달의 계좌 선택(아래)과 예산 화면의 세 입력칸이 같은 이유로 고장났습니다. 후자는 **월을 옮겨도 값이 바뀌지 않아** "이 월 이동 기능은 무엇을 하는 건가요?"라는 신고로 돌아왔습니다 — 예산은 달마다 따로 저장되고 화면의 나머지는 선택한 달을 따라가는데, `useState(초기값)`으로 잡힌 칸만 첫 렌더에 머물러 있었습니다.

**컨텍스트에서 읽어 입력칸에 채우는 값은 예외 없이** 그 값이 바뀔 계기를 의존성으로 한 효과에서 다시 실어야 합니다.

**그리고 그 계기는 `selectedMonth` 가 아니라 `budgetConfig.month` 입니다.** 여기서 두 번째로 물렸습니다 — `selectedMonth` 가 바뀌는 순간 `budgetConfig` 는 **아직 이전 달의 것**이고, 새 달의 설정은 `refreshDbData` 가 DB 에서 읽어 온 뒤에야 도착합니다. 그 사이에 효과가 돌아 칸에 **이전 달 값**을 싣고, 설정이 도착해도 효과는 다시 돌지 않아 그 값이 남았습니다. 같은 8월을 두 번 봤는데 숫자가 다르고 세 칸이 모두 `수정 중` 으로 보인 이유입니다.

→ **비동기로 실려 오는 값을 의존성으로 쓸 때는 "요청한 것"이 아니라 "도착한 것"을 보세요.** 도착 전에는 `settling` 로 두어 `수정 중` 이라고 말하지 않고 저장 버튼·실적 채우기·실적 목록을 잠급니다 — 그 상태에서 쓰면 `updateBudgetConfig` 가 `prev.month`(아직 이전 달)에 기록하므로 **이 달 숫자가 이전 달 행에 적힙니다.**

→ 그리고 **틈 자체를 줄였습니다.** 달이 바뀌면 `FinanceContext` 가 그 달의 예산 설정만 **동기로** 다시 읽습니다(DB 는 이미 열려 있습니다). 전체 새로고침은 `getDatabase()` 를 기다리는 비동기라 몇 프레임이 걸렸고, 그동안 화면에 `값을 읽는 중` 이 보였습니다. `settling` 은 이제 안전장치로만 남습니다.

모달은 대개 **항상 마운트된 채** `isOpen`으로만 여닫힙니다. 그래서 `useState(props.x || 기본값)`의 초기식은 **앱이 처음 뜰 때 한 번** 계산됩니다 — 그 시점에는 부모가 넘길 값이 아직 없습니다. 나중에 prop이 바뀌어도 상태는 그대로입니다.

→ **예외는 `React.lazy` 로 내려받는 창입니다**(§13.3 — 가져오기 창). 늘 마운트돼 있으면 열지 않아도 내려받으므로 **열렸을 때만 그립니다**(`{열렸나 && <Suspense><창 …/></Suspense>}`). 그러면 닫을 때 상태가 함께 사라져 이 함정이 아예 생기지 않습니다 — 대신 **열 때마다 처음부터**입니다. 무거워서 나눌 값이 있는 창에만 쓰세요.

가져오기 모달의 계좌 선택이 그랬습니다. 카드 내역 화면에서 열어도 `defaultAccountId`가 무시되고 늘 첫 계좌가 잡혔습니다. `reset()`도 계좌만은 일부러 남겨 두기 때문에(여러 파일을 연달아 넣으려고) 닫았다 열어도 마찬가지였습니다.

→ **지금은 열 때마다 `defaultAccountId` 로 되돌립니다**(`reset()` 포함). 여러 파일을 연달아 넣는 일은 이제 대기줄이 맡으므로(§7.10) 선택을 남겨 둘 이유가 없어졌고, 남겨 두면 지난번에 고른 카드가 이번 파일에 조용히 붙습니다.

**prop으로 받은 초기값이 있으면 `isOpen`이 켜지는 순간의 효과로 반영하세요.** 의존성은 `[isOpen, 그 prop]`으로 좁혀, 열려 있는 동안의 사용자 선택과 싸우지 않게 합니다.

### 14.8 개발 서버 포트는 데이터의 일부입니다

**`vite.config.ts`의 `server.port`를 바꾸지 마세요.** `3000`으로 고정돼 있고 `strictPort: true`입니다.

이 앱의 모든 데이터는 IndexedDB에 있고, 브라우저는 **출처(origin)별로** 저장소를 나눕니다. **포트가 출처의 일부**이므로 `localhost:3000`과 `localhost:5173`은 브라우저에게 남남입니다. 포트를 지정하지 않아 Vite 기본값 `5173`으로 뜬 적이 있는데, 앱은 **빈 데이터베이스**를 열었고 사용자가 0명이니 규칙대로 **첫 사용자 등록 화면**을 띄웠습니다. 사용자 2명과 등록한 카드·계좌·내역이 **전부 사라진 것처럼** 보였습니다. 하나도 지워지지 않았는데도요.

`strictPort`는 3000이 이미 쓰이고 있을 때 **조용히 3001로 넘어가지 않고 실패**하게 합니다. 말없이 포트가 바뀌는 것이 곧 "데이터가 사라졌다"이기 때문입니다.

- 배포본(Cloudflare 주소)은 또 다른 출처입니다. 개발 서버의 데이터와 서로 보이지 않는 것이 정상입니다.
- 데이터가 안 보인다는 신고를 받으면 **주소창의 포트부터** 확인하세요. DB 카드의 `저장 크기`가 100KB 안팎에 `0건`이면 갓 만들어진 빈 DB입니다.
- 이때 앱이 "빈 가계부"인 척하지 않도록 막아 둔 것이 4.7입니다. 함께 읽으세요.

### 14.9 eslint 가 보는 것

`npm run lint` 는 `tsc --noEmit` 과 **eslint** 를 함께 돌립니다(`eslint.config.js`). 규칙은 좁습니다 — **`tsc` 가 못 보는 것만** 봅니다.

- **훅 규칙**(`react-hooks`). 넣은 까닭은 겪어서입니다: `useEffect` 의 의존성을 잘못 잡아 예산 화면이 이전 달 값을 보여 준 일이 **두 번**(§14.7) 있었고 둘 다 사람 눈으로 찾았습니다.
- **죽은 코드**(`no-unused-vars`). 첫 실행에서 **미사용 import·변수 33곳**을 찾았고, 그 안에 **지어낸 숫자**(`prevMonthExpense = 2470000`, §17.1 위반)와 **제가 만들어 두고 렌더하지 않은 컴포넌트**(저장 실패 띠)가 섞여 있었습니다.
- **`set-state-in-effect` 는 끕니다.** 이 앱은 그 패턴이 **필요합니다** — 비동기로 도착한 값을 입력칸에 싣는 일(§14.7). 33곳이 전부 그 종류라 켜 두면 정작 중요한 경고가 묻힙니다.
- 취향 규칙(따옴표·세미콜론·정렬)은 넣지 않았습니다. 포맷을 다투는 사람이 없고, 경고가 많아지면 중요한 것이 묻힙니다.

**GitHub Actions** (`.github/workflows/check.yml`) 가 `main` 푸시와 PR 마다 `npm run check` + `npm run build` 를 돌립니다. "커밋 전에 돌린다"는 규칙은 사람이 기억해야 하고, 언젠가 잊힙니다.

### 14.10 소스 편집 도구

Python heredoc 문자열 치환으로 소스를 고치다 **`\b`가 실제 0x08 바이트로 들어가** 조용히 기능이 깨진 적이 있습니다. 백슬래시 이스케이프가 들어가는 편집은 heredoc 대신 파일로 스크립트를 쓰거나 편집 도구를 쓰세요. 치환 후에는 대상이 실제로 바뀌었는지 확인하세요(`grep`).

---

---

---

## 16. 커밋

- 한 커밋에 한 가지 변화. 제목은 **무엇을 하는지**(명령형), 본문은 **왜 그렇게 했는지**와 어떤 문제를 고쳤는지.
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- `finance.db`는 `.gitignore`에 있습니다. 저장소에도 빌드 결과물에도 들어가지 않습니다.

---

---

## 17. 앞으로 손댈 때 지킬 것

아래 번호가 곧 절 번호입니다 — 소스 주석의 `§17.5` 는 다섯 번째 항목을 가리킵니다. 이 저장소에서 **가장 많이 참조되는 절**(47곳)이고, 새 코드를 쓰기 전에 읽어야 하는 것도 이것입니다.


1. **데이터를 지어내지 않습니다.** 모르면 `–`로 두고 왜 모르는지 적습니다.
2. **추측으로 연결하지 않습니다.** 카드·명세서·중복 판정 모두, 근거가 없으면 비워 두고 사용자가 정하게 합니다.
3. **기존 데이터를 조용히 버리지 않습니다.** 화면에 없는 필드라도 저장 시 유지하세요(결제월을 날려먹은 적이 있습니다).
4. **구조를 바꾸면 마이그레이션과 `EXPECTED_*` 목록을 함께 고칩니다.**
5. **로직은 `services/`의 순수 함수로**, 화면과 컨텍스트는 그것을 부르기만 하도록.
   → 이 규칙이 실제로 **버그를 드러냅니다.** 화면 안에 있던 계산을 서비스로 옮길 때마다 두 화면이 서로 다른 말을 하고 있던 것이 드러났습니다 — `describeBill`(홈과 카드·계좌가 다른 청구액), `spareOf`(홈이 옛 식을 복제), `categorySpendRows`(목록의 합이 화면 금액과 달랐음).
   → **컨텍스트도 화면입니다.** `FinanceContext` 안의 파생 계산(`categoryStatuses`·`budgetAlertsFor`·`monthlyHistory`·`yearlyHistory`·`categoryBreakdown`)을 내려 회귀 세트가 닿게 했습니다. 남는 것은 상태·저장·조율뿐이어야 합니다.
   → **덩치가 큰 파일은 조각을 나누기보다 산식을 먼저 내립니다.** `AccountLedgerModal`(1,474줄)에서 위험한 것은 JSX 가 아니라 그 안에 숨어 있는 합계·필터·착지 규칙이었습니다 — 카드의 `지출 − 차감` 산식이 **회귀 세트가 닿지 않는 자리**에 있었고, 그것이 틀려 34,000원 사고가 났습니다(§9.5). `services/ledger.ts` 로 내리면서 같은 월 보조 함수가 **세 컴포넌트에 복사**되어 있는 것도 드러나 `services/trend.ts` 로 모았습니다.
   → **`React.memo` 는 컨텍스트를 읽는 컴포넌트에 효과가 없습니다.** `TransactionItem` 이 `useFinance()` 를 부르므로 컨텍스트 값이 바뀌면 어차피 다시 그려집니다. 목록 성능이 문제가 되면 **먼저 핸들러를 prop 으로 내리고** 그다음에 memo 를 붙이세요 — 순서를 거꾸로 하면 아무 효과 없는 코드만 늘어납니다.
6. 새 판정 규칙을 넣을 때는 **가져오기와 마이그레이션이 같은 표를 쓰게** 하세요. 과거 데이터와 새 데이터가 달리 분류되면 합계가 맞지 않습니다.
7. **말이 안 되는 값은 DB 에 닿지 못하게 하고, 이미 닿은 것은 찾아냅니다** (`services/validate.ts` · `services/integrity.ts`).

   실제 기기에서 `2026-26-08` 같은 **불가능한 날짜 46건**이 나왔습니다. 카드 명세서의 두 자리 연도를 읽지 못했던 때(§7.5) 들어온 줄입니다. 그 줄들은
   - 달이 1~12 밖이라 **어느 달 합계에도 들어가지 않고**,
   - 연월 선택 창은 한 해에 12칸만 보여 주므로 **닿을 수 없고**,
   - `내역 찾기` 의 전체 기간으로만 보입니다 — **있는 줄 모르면 영원히 못 찾습니다.**

   → **파서를 고치는 것으로는 모자랍니다.** 고친 곳은 가져오기 한 경로뿐이고, 직접 입력·문자 등록·일괄 수정·되돌리기 복원은 그 규칙을 지나지 않습니다. 규칙을 **한 곳**에 두고 **모든 쓰기가 지나가게** 합니다 — `addTransaction` · `addTransactions` · `updateTransaction` · `updateTransactions` · `importTransactions`(문자 등록이 여기로 옵니다) · 직접 입력 폼.
   → **한 줄이 틀렸다고 전부 막지 않습니다**(`siftTransactions`). 명세서 300줄 중 한 줄 때문에 299줄이 못 들어오면 사용자가 할 수 있는 일은 파일을 버리는 것뿐입니다. 들일 것은 들이고 **못 들인 것을 숫자와 까닭으로** 말합니다(`ImportOutcome.refused` — §7.9와 같은 태도이고, 저장 실패와는 **다른 칸**입니다).
   → 막는 것: 없는 날(2월 30일 포함) · 1~12 밖의 달 · 2000~2100 밖의 연도 · `HH:mm` 아닌 시각 · `YYYY-MM` 아닌 결제월 · 음수/`NaN` 금액 · 빈 계좌·내역명·카테고리 · **방향과 어긋나는 고정비 구분**(수입에 `FIXED` 를 붙이면 고정비 합계가 수입을 셉니다).
   → **`new Date()` 로 날짜를 판정하지 않습니다.** `new Date("2026-26-08")` 은 브라우저마다 다르게 굴러갑니다. 글자를 그대로 봅니다.

   **이미 들어온 것**은 `설정 → 기타 설정 → 데이터 점검` 이 찾습니다(`inspect`). 있을 수 없는 날짜 · 없는 계좌를 가리키는 줄 · 올바르지 않은 결제월 · **카드에 붙은 저축**(§11.4 — 저축은 계좌에서만 세므로 그 금액은 어느 칸에도 안 잡힙니다) · 오늘보다 뒤의 날짜.

   → **지우는 것은 두 조건을 모두 넘을 때뿐입니다**(`zombies`): 날짜가 있을 수 없고, **같은 지출이 제대로 들어간 사본이 다른 곳에 있을 것**. 하나만 맞으면 그 줄은 그 지출의 **유일한 기록**이라 보여 주기만 합니다(§17.3).
     실제 46건이 정확히 그런 경우였습니다 — `KB국민카드` 명세서를 `KB국민은행 입출금` 계좌로 잘못 넣은 한 번의 사고였고(메모가 `할부`·`일시불`·`리볼빙-일시`), 46건 전부가 카드 계좌에 제대로 다시 들어가 있었습니다(합계 1,202,440원).
   → 미래 날짜·카드에 붙은 저축·고아 내역은 **고칠 것이지 버릴 것이 아닙니다.** 앞당겨 적은 거래는 정상이고, 잘못 분류된 저축에는 진짜 지출이 담겨 있고, 고아는 계좌를 다시 등록하면 되살아납니다.
   → 지우기는 **되돌릴 수 있습니다**(§4.9) — 확인 창이 그렇게 말합니다.
   → **원래 어느 달이었을지는 보여 주기만 합니다**(`guessedMonth`). `26.08.03` → `2026-26-08` 이므로 **달 자리에 연도, 날 자리에 달**이 들어갔고, 달은 되살려도 **날은 잃었습니다**. 1일로 채우면 앱이 없는 사실을 적는 것입니다(§17.1).
