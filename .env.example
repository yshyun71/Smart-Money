# 이 파일을 .env 로 복사한 뒤 실제 값을 채워 넣으세요. (.env 는 커밋되지 않습니다)

# GEMINI_API_KEY: /api/ai/* 엔드포인트(절약 분석, SMS 파싱, 코치 챗)에 필요합니다.
# 값이 없으면 서버는 정상 기동하지만 AI 기능만 500 을 반환합니다.
# 발급: https://aistudio.google.com/apikey
GEMINI_API_KEY=""

# DISABLE_HMR: true 로 두면 개발 서버의 핫 리로드와 파일 감시를 끕니다. (선택)
DISABLE_HMR="false"
